import { workflow, node, trigger, languageModel, outputParser, newCredential } from '@n8n/workflow-sdk';

const watchForNewFiles_0 = trigger({
  type: "n8n-nodes-base.googleDriveTrigger",
  version: 1,
  config: { name: "Watch for new files", parameters: {"pollTimes": {"item": [{"mode": "everyMinute"}]}, "triggerOn": "specificFolder", "folderToWatch": {"__rl": true, "value": "1BUG12Po3fytU1bEP6rrb2lU1n9TV826D", "mode": "list", "cachedResultName": "BL DRAFT", "cachedResultUrl": "https://drive.google.com/drive/folders/1BUG12Po3fytU1bEP6rrb2lU1n9TV826D"}, "event": "fileCreated", "options": {"fileType": "all"}}, position: [32, 0], credentials: { googleDriveOAuth2Api: newCredential("Google Drive account 2") } },
  output: [{}]
});

const codeNormalizadorNDeOrdenDesdeElNombre_1 = node({
  type: "n8n-nodes-base.code",
  version: 2,
  config: { name: "Code (normalizador + Nº de orden desde el nombre)", parameters: {"jsCode": "// NODO 2 (Code): normalizador y Nº de orden desde el nombre\nreturn items.map(item => {\n  const j = item.json;\n  const name = (j.name || j.title || \"\").trim();\n\n  // 1) Nº de orden desde nombre (8–12 dígitos antes de _BL o .pdf)\n  const nameNum =\n    name.match(/(\\d{8,12})(?=_?BL\\b|_?bl\\b|\\.pdf$)/)?.[1] ||\n    name.match(/(\\d{8,12})/)?.[1] || null;\n\n  return {\n    json: {\n      fileId: j.id || j.fileId,\n      name,\n      createdTime: j.createdTime || j.modifiedTime,\n      webViewLink: j.webViewLink || j.alternateLink,\n      orden_from_name: nameNum, // <-- Nº de orden detectado del nombre\n      tipo: \"BL_draft\",\n      estado: \"pendiente\"\n    }\n  };\n});\n"}, position: [192, 0] },
  output: [{}]
});

const googleDriveDownload_2 = node({
  type: "n8n-nodes-base.googleDrive",
  version: 3,
  config: { name: "Google Drive (Download)", parameters: {"operation": "download", "fileId": {"__rl": true, "value": "={{$json.fileId}}", "mode": "id"}, "options": {}}, position: [352, 0], credentials: { googleDriveOAuth2Api: newCredential("Google Drive account 2") } },
  output: [{}]
});

const extraerTextoDelPdf_3 = node({
  type: "n8n-nodes-base.extractFromFile",
  version: 1,
  config: { name: "Extraer texto del PDF", parameters: {"operation": "pdf", "options": {"keepSource": "json"}}, position: [512, 0] },
  output: [{}]
});

const detector_4 = node({
  type: "n8n-nodes-base.code",
  version: 2,
  config: { name: "Detector", parameters: {"jsCode": "// NODO 3 – Detector mínimo (orden + naviera, con booking para LOG-IN)\n// Entradas por item: json.name (ej. \"117779698_BL.pdf\"), json.text (texto del PDF)\n// Salidas: json.order_number, json.order_match, json.booking_no, json.carrier_code, json.carrier_name, json.carrier_evidence\n\nfunction norm(s){ return (s||\"\").replace(/\\r/g,\" \").replace(/[ \\t]+/g,\" \").trim(); }\n\n// 1) Nº de orden — SIEMPRE desde el nombre del archivo (8–12 dígitos)\nfunction orderFromName(name){\n  const m = (name||\"\").match(/(\\d{8,12})(?=[_.-]?BL\\b|\\.pdf$)|(\\d{8,12})/i);\n  return m ? (m[1]||m[2]) : null;\n}\n\n// 1.1) Validar si el nº de orden aparece en el PDF (prioriza ventana cerca de \"Export References\"; filtra CUIT/CNPJ)\nfunction orderAppearsInText(txt, order){\n  if (!order) return null;\n  const t = norm(txt);\n  const reNum = new RegExp(`\\\\b${order}\\\\b`);\n  // Ventanas cerca de \"Export References\"\n  let windows = [];\n  for (const m of t.matchAll(/(EXPORT\\s*REFERENCES?)/gi)) {\n    windows.push(t.slice(m.index, m.index + 260));\n  }\n  if (windows.length === 0) windows.push(t);\n  for (const win of windows) {\n    if (reNum.test(win) && !/(CUIT|CNPJ|TAX\\s*ID|RUC)/i.test(win)) return true;\n  }\n  // Global fallback (con filtro fiscal alrededor)\n  const idx = t.search(reNum);\n  if (idx >= 0) {\n    const around = t.slice(Math.max(0, idx-20), idx+String(order).length+20);\n    if (!/(CUIT|CNPJ|TAX\\s*ID|RUC)/i.test(around)) return true;\n  }\n  return false;\n}\n\n// 2) Booking (para detectar LOG-IN por prefijo LA/LM)\nfunction readBookingLine(txt){\n  const T = (txt||\"\").replace(/\\r/g,\"\");\n  const m = T.match(/BOOKING\\s*NO\\.?:?\\s*([^\\n]+)/i);\n  return m ? m[1].trim() : null;\n}\nfunction bookingToken(bookingLine){\n  if (!bookingLine) return null;\n  const token = (bookingLine.match(/[A-Z]{2}\\d{4,}/i)||[])[0] || null;\n  return token ? token.toUpperCase() : null;\n}\n\n// 3) Carrier (naviera) — sin usar “Sea Waybill” como indicador\nfunction stripVesselVoyageBlocks(t){\n  // Evita falsos positivos por nombres de buque (p.ej. \"MERCOSUL SUAPE\")\n  let out = t;\n  out = out.replace(/VESSEL(?:\\s*NAME|\\(s\\))?\\s*[:\\s].{0,140}/ig, \" \");\n  out = out.replace(/VOYAGE\\s*[:\\s].{0,80}/ig, \" \");\n  return out;\n}\nfunction carrierDetect(txt){\n  const t = norm(txt);\n  const bookingLine = readBookingLine(t);\n  const bk = bookingToken(bookingLine);\n\n  // Regla PRIORITARIA: si el booking empieza con LA o LM => LOG-IN\n  if (bk && /^L[AM]\\d{4,}$/.test(bk)) {\n    return { code: \"LOG-IN\", name: \"Log-In\", evidence: `booking_prefix:${bk}` };\n  }\n\n  // Evidencia fuerte: \"Signed by the Carrier: ...\"\n  const mSigned = t.match(/Signed\\s+by\\s+the\\s+Carrier\\s*[:\\s-]*([^\\n]+)/i);\n  if (mSigned){\n    const s = mSigned[1].toUpperCase();\n    if (s.includes(\"HAPAG\")) return {code:\"HAPAG-LLOYD\", name:\"Hapag-Lloyd\", evidence:`signed:${mSigned[1]}`};\n    if (s.includes(\"SEALAND\")) return {code:\"SEALAND\", name:\"Sealand\", evidence:`signed:${mSigned[1]}`};\n    if (s.includes(\"MAERSK\")) return {code:\"MAERSK\", name:\"Maersk\", evidence:`signed:${mSigned[1]}`};\n    if (s.includes(\"MERCOSUL\")) return {code:\"MERCOSUL\", name:\"Mercosul\", evidence:`signed:${mSigned[1]}`};\n    if (s.includes(\"LOG-IN\") || s.includes(\"LOGIN\")) return {code:\"LOG-IN\", name:\"Log-In\", evidence:`signed:${mSigned[1]}`};\n  }\n\n  // Fallback por marcas, EXCLUYENDO “Sea Waybill” y EXCLUYENDO bloques de Vessel/Voyage\n  const body = stripVesselVoyageBlocks(t).replace(/Sea\\s*Waybill/ig, \" \"); // <- EXCLUIDO a pedido\n  if (/Hapag[-\\s]?Lloyd/i.test(body)) return {code:\"HAPAG-LLOYD\", name:\"Hapag-Lloyd\", evidence:\"brand:Hapag-Lloyd\"};\n  if (/\\bSealand\\b/i.test(body))      return {code:\"SEALAND\",     name:\"Sealand\",     evidence:\"brand:Sealand\"};\n  if (/\\bMAERSK\\b|\\bSCAC\\s+MAEU\\b/i.test(body))\n                                      return {code:\"MAERSK\",      name:\"Maersk\",      evidence:\"brand:Maersk/MAEU\"};\n  if (/\\bMercosul\\b/i.test(body))     return {code:\"MERCOSUL\",    name:\"Mercosul\",    evidence:\"brand:Mercosul\"};\n  if (/\\bLOG[-\\s]?IN\\b/i.test(body))  return {code:\"LOG-IN\",      name:\"Log-In\",      evidence:\"brand:Log-In\"};\n\n  return {code:null, name:null, evidence:\"none\"};\n}\n\nreturn items.map(item => {\n  const j = item.json || {};\n  const name = j.name || j.filename || \"\";\n  const text = j.text || \"\";\n\n  const order_number = orderFromName(name);\n  const match = orderAppearsInText(text, order_number);\n  const bookingLine = readBookingLine(text);\n  const carrier = carrierDetect(text);\n\n  return {\n    json: {\n      ...j,\n      order_number,\n      order_match: (match === null ? null : !!match),\n      booking_no: bookingLine || null,\n      carrier_code: carrier.code,\n      carrier_name: carrier.name,\n      carrier_evidence: carrier.evidence\n    }\n  };\n});\n"}, position: [672, 0] },
  output: [{}]
});

const switchRuteoPorNavieraValidacinDeOrden_5 = node({
  type: "n8n-nodes-base.switch",
  version: 3.2,
  config: { name: "Switch (ruteo por naviera + validación de orden)", parameters: {"mode": "expression", "numberOutputs": 7, "output": "={{\n  $json.order_match === false ? 0 :\n  $json.carrier_code === 'LOG-IN' ? 1 :\n  $json.carrier_code === 'MERCOSUL' ? 2 :\n  $json.carrier_code === 'MAERSK' ? 3 :\n  $json.carrier_code === 'SEALAND' ? 4 :\n  $json.carrier_code === 'HAPAG-LLOYD' ? 5 :\n  6\n}}\n"}, position: [832, -80] },
  output: [{}]
});

const extractLogInCode_6 = node({
  type: "n8n-nodes-base.code",
  version: 2,
  config: { name: "Extract — LOG-IN (Code).", parameters: {"jsCode": "/**\n * NODO 5 — Extractor LOG-IN (Code node, JavaScript)\n *\n * Entrada por item (desde Switch → LOG-IN):\n *   - json.text            → texto plano del PDF\n *   - json.order_number    → número de orden (desde detector)\n *   - json.booking_no      → booking (desde detector)\n *   - json.carrier_code    → \"LOG-IN\"\n *   - (opcional) name, fileId, webViewLink\n *\n * Salida:\n *   - json.login_extract   → objeto estructurado\n *   - json.excel_pairs     → [{ Dato, Valor }]\n */\n\nfunction T(s){ return (s || \"\").replace(/\\r/g,\"\"); }\nfunction normLine(s){ return (s || \"\").replace(/[ \\t]+/g,\" \").trim(); }\nfunction upperKeepNL(s){ return (s || \"\").toUpperCase(); }\nfunction parseNumberEU(s){\n  if (s == null) return null;\n  const z = String(s).replace(/[^\\d.,-]/g,\"\");\n  if (!z) return null;\n  const val = parseFloat(z.replace(/\\./g,\"\").replace(\",\",\".\"));\n  return isNaN(val) ? null : val;\n}\nfunction moneyTokenRe(){ return /(US\\$|U\\$S|USD|R\\$)\\s*([\\d.,]+)/ig; }\nfunction toCUR(sym){ return /R\\$/i.test(sym) ? \"BRL\" : \"USD\"; }\n\n// Línea siguiente (LOG-IN pone valores en la línea inferior)\nfunction afterNextLine(text, labelRe){\n  const lines = T(text).split(\"\\n\");\n  for (let i=0;i<lines.length;i++){\n    if (labelRe.test(lines[i])){\n      for (let j=i+1;j<lines.length;j++){\n        const ln = normLine(lines[j]);\n        if (ln){ return ln; }\n      }\n      return \"\";\n    }\n  }\n  return \"\";\n}\n\n// Export references: SOLO de la línea siguiente a (6) y filtrado\nfunction findExportRefsStrict(text){\n  const line = afterNextLine(text, /\\(6\\)\\s*EXPORT\\s+REFERENCES/i);\n  if (!line) return [];\n  const nums = (line.match(/\\b\\d{7,12}\\b/g) || []);\n  const out = [];\n  for (const n of nums){\n    const idx = line.indexOf(n);\n    const around = line.slice(Math.max(0, idx-10), idx+10);\n    if (/(CNPJ|CUIT|RUC|TAX\\s*ID)/i.test(around)) continue;\n    out.push(n);\n  }\n  return [...new Set(out)];\n}\n\n// DESCRIPTION (bloque base y multi-ítems)\nfunction parseDescription(text){\n  const t = T(text);\n  let startIdx = t.search(/PARTICULARS\\s+FURNISHED\\s+BY\\s+SHIPPER/i);\n  if (startIdx < 0){\n    startIdx = t.search(/DESCRIPTION\\s+OF\\s+PACKAGES\\s+AND\\s+GOODS/i);\n  }\n  if (startIdx < 0) return {};\n\n  const tail = t.slice(startIdx, startIdx+8000);\n  let endIdx = tail.search(/FREIGHT\\s+CHARGES|Container\\s+Seal\\s+Type|^\\s*\\(\\d+\\)\\s/im);\n  if (endIdx < 0) endIdx = tail.length;\n  const rawBlock = tail.slice(0, endIdx);\n\n  const U = upperKeepNL(rawBlock.replace(/\\n+/g, \" \").replace(/[ \\t]+/g,\" \").trim());\n\n  // Contenedores: 4 X 40HC / 04 CONTAINERS OF 40 HC\n  let cantCont = null, tipoCont = \"\";\n  let m = U.match(/\\b(\\d+)\\s*[Xx]\\s*(\\d+)\\s*HC\\b/);\n  if (m){ cantCont = parseInt(m[1],10); tipoCont = `${m[2]} HC`; }\n  if (cantCont == null){\n    m = U.match(/\\b(\\d+)\\s+CONTAINERS?\\s+OF\\s+(\\d+)\\s*HC\\b/);\n    if (m){ cantCont = parseInt(m[1],10); tipoCont = `${m[2]} HC`; }\n  }\n\n  // NCM, madera, PE (ojo: condición de madera debe cortar antes de PE)\n  const NCM       = ((U.match(/\\bNCM:\\s*([0-9.]{4,})/) || [])[1] || \"\").replace(/\\./g,\"\");\n  const WOOD_YN   = (U.match(/WOODEN\\s+MATERIAL:\\s*(YES|NO)/) || [])[1] || \"\";\n  const WOOD_COND = (U.match(/WOODEN\\s+CONDITION:\\s*([A-Z ]+?)(?=\\s+PE\\b|$)/) || [])[1] || \"\";\n  const PE_CODE   = (U.match(/\\bPE\\s+([A-Z0-9]+)\\b/) || [])[1] || \"\";\n\n  // GOODS global (hasta QUANTITY) como string “crudo”\n  let goodsRaw = \"\";\n  const g1 = U.match(/GOODS:\\s*([^]*?)(?=\\bQUANTITY\\b|$)/i);\n  if (g1){ goodsRaw = normLine(g1[1]); }\n\n  function deriveFromGoods(gl){\n    let PRODUCTO=\"\", DENSIDAD=\"\", GRADE=\"\", EMBALAJE=\"\", PESO_BOLSA_KG=null;\n    if (gl){\n      if (/\\bPE\\b/.test(gl)) PRODUCTO = \"POLYETHYLENE\";\n      else {\n        const prodTok = (gl.split(/\\s+/).find(tk => /^[A-Z]+$/.test(tk)) || \"\");\n        if (prodTok) PRODUCTO = prodTok;\n      }\n      if (/\\bHDPE\\b/.test(gl) || /\\bHIGH\\s+DENSITY\\b/.test(gl)) DENSIDAD = \"HIGH DENSITY\";\n      else if (/\\bLDPE\\b/.test(gl) || /\\bLOW\\s+DENSITY\\b/.test(gl)) DENSIDAD = \"LOW DENSITY\";\n      const gradeTok = (gl.match(/\\b[A-Z0-9]*\\d[A-Z0-9]*\\b/g) || []).find(x=>/[0-9]/.test(x));\n      if (gradeTok) GRADE = gradeTok;\n      if (/BIG\\s+BAG/.test(gl)) { EMBALAJE = \"BIG BAG\"; }\n      const mBB = gl.match(/\\bBB\\s*([0-9]{2,5})\\b/);\n      const mKG = gl.match(/(\\d{2,5})\\s*KG\\b/);\n      if (mBB) { PESO_BOLSA_KG = parseInt(mBB[1],10); EMBALAJE = \"BIG BAG\"; }\n      else if (mKG) { PESO_BOLSA_KG = parseInt(mKG[1],10); if (!EMBALAJE) EMBALAJE = \"BAG\"; }\n    }\n    return {PRODUCTO, DENSIDAD, GRADE, EMBALAJE, PESO_BOLSA_KG};\n  }\n  const derived = deriveFromGoods(goodsRaw);\n\n  // Multi-ítems\n  const items = [];\n  const B = upperKeepNL(rawBlock); // con saltos\n  const reItem = /GOODS:\\s*([^\\n]+?)\\s*(?:\\n+| )QUANTITY:\\s*([0-9.,]+)\\s*BAGS?\\s*IN\\s*([0-9.,]+)\\s*PALLETS?.*?(?:\\n+| )GROSS\\s+WEIGHT:\\s*([0-9.,]+).*?(?:\\n+| )NET\\s+WEIGHT:\\s*([0-9.,]+)/g;\n  let im;\n  while ((im = reItem.exec(B)) !== null){\n    const goods_i = normLine(im[1]);\n    const bags = parseNumberEU(im[2]) || 0;\n    const pallets = parseNumberEU(im[3]) || 0;\n    const gross = parseNumberEU(im[4]) || 0;\n    const net   = parseNumberEU(im[5]) || 0;\n    const d = deriveFromGoods(goods_i);\n    items.push({\n      goodsRaw: goods_i,\n      PRODUCTO: d.PRODUCTO,\n      DENSIDAD: d.DENSIDAD,\n      GRADE: d.GRADE,\n      EMBALAJE: d.EMBALAJE,\n      PESO_BOLSA_KG: d.PESO_BOLSA_KG,\n      BOLSAS: bags, PALLETS: pallets, GROSS: gross, NET: net\n    });\n  }\n\n  const totals = { BOLSAS:0, PALLETS:0, GROSS:0, NET:0 };\n  if (items.length){\n    for (const it of items){\n      totals.BOLSAS += it.BOLSAS || 0;\n      totals.PALLETS += it.PALLETS || 0;\n      totals.GROSS += it.GROSS || 0;\n      totals.NET += it.NET || 0;\n    }\n  }\n\n  let CANT_BOLSAS = null, CANT_PALLETS = null;\n  if (!items.length){\n    const q1 = B.match(/QUANTITY:\\s*([0-9.,]+)\\s*BAGS?\\s*IN\\s*([0-9.,]+)\\s*PALLETS?/i);\n    if (q1){\n      CANT_BOLSAS = parseNumberEU(q1[1]);\n      CANT_PALLETS = parseNumberEU(q1[2]);\n    }\n  }\n\n  const GROSS_TOT = parseNumberEU((U.match(/GROSS\\s+WEIGHT:\\s*([0-9.,]+)/) || [])[1]);\n  const NET_TOT   = parseNumberEU((U.match(/NET\\s+WEIGHT:\\s*([0-9.,]+)/)   || [])[1]);\n\n  return {\n    cantCont, tipoCont, goodsRaw,\n    PRODUCTO: derived.PRODUCTO, DENSIDAD: derived.DENSIDAD, GRADE: derived.GRADE,\n    EMBALAJE: derived.EMBALAJE, PESO_BOLSA_KG: derived.PESO_BOLSA_KG,\n    NCM, WOOD_YN, WOOD_COND, PE_CODE,\n    items, totals,\n    CANT_BOLSAS, CANT_PALLETS,\n    GROSS_TOT, NET_TOT\n  };\n}\n\n// Tabla de contenedores → Equipos (normalizando Type a “NN HC”)\nfunction parseEquipos(text){\n  const lines = T(text).split(\"\\n\");\n  let hdr = -1;\n  for (let i=0;i<lines.length;i++){\n    if (/^\\s*Container\\s+Seal\\s+Type\\s+Tare\\s+G\\.?W\\s+N\\.?W\\s+Measurement\\s+Wooden\\s+Material\\s+Wooden\\s+Conditions/i.test(lines[i])){ hdr = i; break; }\n  }\n  const out = [];\n  if (hdr < 0) return out;\n  const isTerm = s => !s.trim() || /^FREIGHT CHARGES/i.test(s) || /^\\s*\\d+\\s+of\\s+\\d+/i.test(s);\n  let i = hdr + 1;\n  while (i < lines.length){\n    let ln = lines[i];\n    if (isTerm(ln)) break;\n    const m = ln.match(/^\\s*([A-Z]{4}\\d{7})\\b(.*)$/);\n    if (m){\n      let row = (m[1] + \" \" + m[2].trim());\n      let j = i+1;\n      while (j < lines.length){\n        const nx = lines[j];\n        if (/^\\s*[A-Z]{4}\\d{7}\\b/.test(nx) || isTerm(nx)) break;\n        row += \" \" + nx.trim();\n        j++;\n      }\n      const norm = row.replace(/\\s+/g,\" \").trim();\n      const m2 = norm.match(/^([A-Z]{4}\\d{7})\\s+([A-Z0-9-]+)\\s+([A-Z0-9]+)\\s+(\\d+)\\s+([0-9.,]+)\\s+([0-9.,]+)\\s+([0-9.,]+)\\s+(Yes|No)\\s+(.+)$/i);\n      if (m2){\n        let type = m2[3];\n        const mHC = type.match(/(\\d+)\\s*HC/i);\n        if (mHC) type = `${mHC[1]} HC`;\n        else if (/^(\\d{2})HC$/i.test(type)) type = type.replace(/^(\\d{2})HC$/i, \"$1 HC\");\n        out.push({\n          container: m2[1],\n          seal: m2[2],\n          type,\n          tare: m2[4],\n          gw: m2[5],\n          nw: m2[6],\n          meas: m2[7],\n          wood: m2[8],\n          wood_cond: m2[9]\n        });\n      }\n      i = j;\n    } else { i++; }\n  }\n  return out;\n}\n\n// FREIGHT CHARGES por columnas (usa totales para clasificar PREPAID/COLLECT)\nfunction parseFreightByColumns(text){\n  const t = T(text);\n  const start = t.search(/FREIGHT\\s+CHARGES\\s+RATED\\s+AS\\s+PER\\s+RATE\\s+PREPAID\\s+COLLECT/i);\n  if (start < 0) return { concepts:[], totals:{ USD:{prepaid:0,collect:0}, BRL:{prepaid:0,collect:0} } };\n  const tail = t.slice(start, start+3000);\n  const lines = tail.split(\"\\n\").map(normLine).filter(Boolean);\n\n  // Totales por moneda (líneas sueltas con 2 importes)\n  const totals = { USD:{prepaid:0,collect:0}, BRL:{prepaid:0,collect:0} };\n  for (const ln of lines){\n    const mm = [...ln.matchAll(moneyTokenRe())];\n    if (mm.length === 2 && ln.replace(moneyTokenRe(),\"\").trim()===\"\"){\n      const cur1 = toCUR(mm[0][1]), v1 = parseNumberEU(mm[0][2]);\n      const cur2 = toCUR(mm[1][1]), v2 = parseNumberEU(mm[1][2]);\n      if (cur1 === cur2){ totals[cur1].prepaid = v1||0; totals[cur1].collect = v2||0; }\n    }\n  }\n\n  const concepts = [];\n  const rem = { USD:{prepaid:totals.USD.prepaid, collect:totals.USD.collect},\n                BRL:{prepaid:totals.BRL.prepaid, collect:totals.BRL.collect} };\n\n  for (const ln of lines){\n    if (/^FREIGHT\\s+CHARGES/i.test(ln)) continue;\n    const mmo = [...ln.matchAll(moneyTokenRe())];\n    if (mmo.length === 2 && ln.replace(moneyTokenRe(),\"\").trim()===\"\"){ continue; } // totales\n\n    // Concepto = texto antes del primer monto, limpiando \"4,00 EACH\"\n    let concept = ln;\n    const idxMoney = ln.search(moneyTokenRe());\n    if (idxMoney > 0) concept = ln.slice(0, idxMoney).trim();\n    concept = concept.replace(/\\b\\d+[.,]?\\d*\\s+EACH\\b/i, \"\").trim();\n\n    if (mmo.length === 0) continue;\n    const rateSym = mmo[0][1], rateAmt = parseNumberEU(mmo[0][2]), rateCur = toCUR(rateSym);\n    const last = mmo[mmo.length-1];\n    const lastCur = toCUR(last[1]); const lastAmt = parseNumberEU(last[2]);\n\n    let kind = null;\n    if (lastAmt != null){\n      const canPre = lastAmt <= (rem[lastCur]?.prepaid ?? 0);\n      const canCol = lastAmt <= (rem[lastCur]?.collect ?? 0);\n      if (canPre && (!canCol || lastAmt === (rem[lastCur]?.prepaid ?? 0))) {\n        kind = \"PREPAID\";\n        rem[lastCur].prepaid -= lastAmt;\n      } else if (canCol){\n        kind = \"COLLECT\";\n        rem[lastCur].collect -= lastAmt;\n      } else {\n        kind = /ORIGEM/i.test(concept) ? \"PREPAID\" : \"COLLECT\";\n      }\n    }\n\n    concepts.push({\n      concept,\n      kind,\n      currency: lastCur,\n      amount: lastAmt,\n      rate_currency: rateCur,\n      rate: rateAmt\n    });\n  }\n\n  return { concepts, totals };\n}\n\nreturn items.map(item => {\n  const j = item.json || {};\n  const text = j.text || \"\";\n\n  // Identificación y ruta (línea siguiente)\n  const bl_no = afterNextLine(text, /\\(5A\\)\\s*BILL\\s+OF\\s+LADING\\s+NO\\.?/i);\n  const vesselVoy = afterNextLine(text, /\\(14\\)\\s*VESSEL\\s*VOYAGE/i);\n  let vessel = \"\", voyage = \"\";\n  if (vesselVoy.includes(\"/\")){\n    const [a,b] = vesselVoy.split(\"/\",2).map(s=>normLine(s));\n    vessel = a; voyage = b;\n  } else { vessel = vesselVoy; }\n\n  const pol = afterNextLine(text, /\\(15\\)\\s*PORT\\s+OF\\s+LOADING/i);\n  const pod = afterNextLine(text, /\\(16\\)\\s*PORT\\s+OF\\s+DISCHARGE/i);\n  const place_delivery = afterNextLine(text, /\\(17\\)\\s*PLACE\\s+OF\\s+DELIVERY/i);\n\n  // Export references (estricto)\n  const export_refs = findExportRefsStrict(text);\n\n  // Bloques multilínea básicos (Ship/Cons/Notify)\n  function blockAfter(text, startRe, stopRes, maxlen){\n    const lines = T(text).split(\"\\n\");\n    let si = -1;\n    for (let i=0;i<lines.length;i++){ if (startRe.test(lines[i])) { si=i; break; } }\n    if (si<0) return \"\";\n    const buf = [];\n    for (let k=si+1; k<lines.length && buf.join(\"\\n\").length < (maxlen||2000); k++){\n      const l = lines[k];\n      if (stopRes.some(r => r.test(l))) break;\n      const n = normLine(l);\n      if (n) buf.push(n);\n    }\n    return buf.filter(l => !/PARTICULARS FURNISHED BY SHIPPER/i.test(l)).join(\"\\n\");\n  }\n  const shipper  = blockAfter(text, /\\(2\\)\\s*SHIPPER\\/EXPORTER[^\\n]*/i, [/^\\s*\\(\\d+\\)/, /BOOKING/i, /BILL\\s+OF\\s+LADING/i, /VESSEL/i, /PORT\\s+OF/i], 2000);\n  const consignee= blockAfter(text, /\\(3\\)\\s*CONSIGNEE[^\\n]*/i, [/^\\s*\\(\\d+\\)/, /NOTIFY/i, /FORWARDING/i, /BOOKING/i, /BILL\\s+OF\\s+LADING/i, /VESSEL/i, /PORT\\s+OF/i], 2000);\n  const notify   = blockAfter(text, /\\(4\\)\\s*NOTIFY\\s*PARTY[^\\n]*|\\bNOTIFY\\s*PARTY[^\\n]*/i, [/^\\s*\\(\\d+\\)/, /FORWARDING/i, /BOOKING/i, /BILL\\s+OF\\s+LADING/i, /VESSEL/i, /PORT\\s+OF/i, /\\bCONSIGNEE\\b/i, /\\bSHIPPER\\b/i], 2000);\n\n  // DESCRIPTION (unificada + multi-ítem)\n  const D = parseDescription(text);\n\n  // Equipos\n  const equipos = parseEquipos(text);\n\n  // Freight por columnas (usando totales para clasificar)\n  const F = parseFreightByColumns(text);\n\n  // ========= CAMBIO: Tarifa por contenedor según Ocean Freight (PREPAID o COLLECT) =========\n  const cntCont = (D.cantCont != null ? D.cantCont : (equipos.length || 0));\n\n  // Detectar el KIND del concepto \"Ocean Freight\"\n  let oceanFreightKind = \"\";\n  for (const c of (F.concepts || [])){\n    const name = (c.concept || \"\").toUpperCase();\n    if (name.includes(\"OCEAN\") && name.includes(\"FREIGHT\")){\n      if (c.kind === \"PREPAID\" || c.kind === \"COLLECT\") {\n        oceanFreightKind = c.kind;\n        break;\n      }\n    }\n  }\n\n  const totalUSDpre = F?.totals?.USD?.prepaid || 0;\n  const totalUSDcol = F?.totals?.USD?.collect || 0;\n\n  let perUSDpre = 0;\n  let perUSDcol = 0;\n\n  if (cntCont > 0) {\n    if (oceanFreightKind === \"PREPAID\") {\n      perUSDpre = +(totalUSDpre / cntCont).toFixed(2);\n      perUSDcol = 0;\n    } else if (oceanFreightKind === \"COLLECT\") {\n      perUSDpre = 0;\n      perUSDcol = +(totalUSDcol / cntCont).toFixed(2);\n    }\n  }\n  const perUSD = perUSDpre || perUSDcol || 0;\n  // ========= FIN CAMBIO =========\n\n  // ----- Objeto final -----\n  const login_extract = {\n    order_number: j.order_number || null,\n    booking_no: j.booking_no || null,\n    bl_no: bl_no || null,\n    export_references: export_refs,\n    carrier: \"LOG-IN\",\n    vessel: upperKeepNL(vessel),\n    voyage: upperKeepNL(voyage),\n    pol: upperKeepNL(pol),\n    pod: upperKeepNL(pod),\n    place_delivery: upperKeepNL(place_delivery),\n    shipper: upperKeepNL(shipper),\n    consignee: upperKeepNL(consignee),\n    notify: upperKeepNL(notify),\n    desc: {\n      \"DESC BL - CANTIDAD DE CONTENEDORES\": D.cantCont ?? null,\n      \"DESC BL - TIPO DE CONTENEDOR\": upperKeepNL(D.tipoCont || \"\"),\n      \"DESC BL - GOODS (DESCRIPCIÓN CRUDA)\": upperKeepNL(D.goodsRaw || \"\"),\n      \"DESC BL - PRODUCTO\": upperKeepNL(D.PRODUCTO || \"\"),\n      \"DESC BL - GRADE / CALIDAD\": upperKeepNL(D.GRADE || \"\"),\n      \"DESC BL - DENSIDAD / TIPO\": upperKeepNL(D.DENSIDAD || \"\"),\n      \"DESC BL - TIPO DE EMBALAJE\": upperKeepNL(D.EMBALAJE || \"\"),\n      \"DESC BL - PESO POR BOLSA (KG)\": D.PESO_BOLSA_KG ?? null,\n      \"DESC BL - CANTIDAD DE BOLSAS\": (D.items && D.items.length) ? D.totals.BOLSAS : (D.CANT_BOLSAS ?? null),\n      \"DESC BL - CANTIDAD DE PALLETS\": (D.items && D.items.length) ? D.totals.PALLETS : (D.CANT_PALLETS ?? null),\n      \"DESC BL - NCM\": upperKeepNL(D.NCM || \"\"),\n      \"DESC BL - PESO BRUTO TOTAL (KG)\": (D.items && D.items.length) ? D.totals.GROSS : (D.GROSS_TOT ?? null),\n      \"DESC BL - PESO NETO TOTAL (KG)\": (D.items && D.items.length) ? D.totals.NET : (D.NET_TOT ?? null),\n      \"DESC BL - MADERA (¿USA?)\": upperKeepNL(D.WOOD_YN || \"\"),\n      \"DESC BL - CONDICIÓN DE MADERA\": upperKeepNL(D.WOOD_COND || \"\"),\n      \"DESC BL - PE (PERMISO DE EMBARQUE)\": upperKeepNL(D.PE_CODE || \"\")\n    },\n    desc_items: (D.items || []).map((it,idx)=>({\n      index: idx+1,\n      goods_raw: upperKeepNL(it.goodsRaw || \"\"),\n      PRODUCTO: upperKeepNL(it.PRODUCTO || \"\"),\n      DENSIDAD: upperKeepNL(it.DENSIDAD || \"\"),\n      GRADE: upperKeepNL(it.GRADE || \"\"),\n      EMBALAJE: upperKeepNL(it.EMBALAJE || \"\"),\n      PESO_BOLSA_KG: it.PESO_BOLSA_KG ?? null,\n      BOLSAS: it.BOLSAS ?? null,\n      PALLETS: it.PALLETS ?? null,\n      GROSS: it.GROSS ?? null,\n      NET: it.NET ?? null\n    })),\n    equipos,\n    freight: {\n      concepts: F.concepts,          // Detalle por concepto\n      totals: F.totals,              // Totales por moneda\n      ocean_freight_kind: oceanFreightKind, // PREPAID | COLLECT | \"\"\n      per_container: {               // Para la plantilla (fila TARIFA POR CONTENEDOR)\n        USD_prepaid: perUSDpre,\n        USD_collect: perUSDcol,\n        USD: perUSD,                 // compatibilidad\n      },\n      containers_for_calc: cntCont\n    }\n  };\n\n  // ----- Pares para Excel -----\n  const pairs = [];\n  pairs.push({ Dato: \"Nº ORDEN (EXPORT REFERENCES)\", Valor: login_extract.order_number || (export_refs[0] || \"\") });\n  pairs.push({ Dato: \"BOOKING NO\", Valor: login_extract.booking_no || \"\" });\n  pairs.push({ Dato: \"BL / SWB NO\", Valor: login_extract.bl_no || \"\" });\n  pairs.push({ Dato: \"CARRIER (NAVIERA)\", Valor: \"LOG-IN\" });\n  pairs.push({ Dato: \"VESSEL (BUQUE)\", Valor: login_extract.vessel });\n  pairs.push({ Dato: \"VOYAGE\", Valor: login_extract.voyage });\n  pairs.push({ Dato: \"PORT OF LOADING (POL)\", Valor: login_extract.pol });\n  pairs.push({ Dato: \"PORT OF DISCHARGE (POD)\", Valor: login_extract.pod });\n  pairs.push({ Dato: \"PLACE OF DELIVERY\", Valor: login_extract.place_delivery });\n  pairs.push({ Dato: \"SHIPPER\", Valor: login_extract.shipper });\n  pairs.push({ Dato: \"CONSIGNEE\", Valor: login_extract.consignee });\n  pairs.push({ Dato: \"NOTIFY\", Valor: login_extract.notify });\n\n  // DESC BL (totales / globales)\n  for (const k of Object.keys(login_extract.desc)){\n    pairs.push({ Dato: k, Valor: (login_extract.desc[k] ?? \"\").toString() });\n  }\n\n  // DESC Ítems (si hay)\n  (login_extract.desc_items || []).forEach(it => {\n    const n = it.index;\n    pairs.push({ Dato: `DESC BL - ÍTEM ${n} - GOODS (CRUDO)`, Valor: it.goods_raw });\n    pairs.push({ Dato: `DESC BL - ÍTEM ${n} - PRODUCTO`, Valor: it.PRODUCTO });\n    pairs.push({ Dato: `DESC BL - ÍTEM ${n} - DENSIDAD / TIPO`, Valor: it.DENSIDAD });\n    pairs.push({ Dato: `DESC BL - ÍTEM ${n} - GRADE / CALIDAD`, Valor: it.GRADE });\n    pairs.push({ Dato: `DESC BL - ÍTEM ${n} - TIPO DE EMBALAJE`, Valor: it.EMBALAJE });\n    pairs.push({ Dato: `DESC BL - ÍTEM ${n} - PESO POR BOLSA (KG)`, Valor: String(it.PESO_BOLSA_KG ?? \"\") });\n    pairs.push({ Dato: `DESC BL - ÍTEM ${n} - CANTIDAD DE BOLSAS`, Valor: String(it.BOLSAS ?? \"\") });\n    pairs.push({ Dato: `DESC BL - ÍTEM ${n} - CANTIDAD DE PALLETS`, Valor: String(it.PALLETS ?? \"\") });\n    pairs.push({ Dato: `DESC BL - ÍTEM ${n} - GROSS WEIGHT (KG)`, Valor: String(it.GROSS ?? \"\") });\n    pairs.push({ Dato: `DESC BL - ÍTEM ${n} - NET WEIGHT (KG)`, Valor: String(it.NET ?? \"\") });\n  });\n\n  if ((login_extract.desc_items || []).length > 1){\n    pairs.push({ Dato: `DESC BL - TOTALES - CANTIDAD DE BOLSAS`, Valor: String(D.totals.BOLSAS) });\n    pairs.push({ Dato: `DESC BL - TOTALES - CANTIDAD DE PALLETS`, Valor: String(D.totals.PALLETS) });\n    pairs.push({ Dato: `DESC BL - TOTALES - GROSS WEIGHT (KG)`, Valor: String(D.totals.GROSS) });\n    pairs.push({ Dato: `DESC BL - TOTALES - NET WEIGHT (KG)`, Valor: String(D.totals.NET) });\n  }\n\n  // Freight resumen (para Excel)\n  const nCont = login_extract.freight.containers_for_calc || 0;\n  pairs.push({ Dato: \"FLETE BL - Nº CONTENEDORES (para cálculo)\", Valor: String(nCont) });\n  pairs.push({ Dato: \"FLETE BL - OCEAN FREIGHT KIND\", Valor: login_extract.freight.ocean_freight_kind || \"\" });\n  pairs.push({ Dato: \"FLETE BL - TARIFA POR CONTENEDOR PREPAID (US$)\", Valor: (login_extract.freight.per_container.USD_prepaid || 0).toLocaleString(\"es-AR\",{minimumFractionDigits:2}) });\n  pairs.push({ Dato: \"FLETE BL - TARIFA POR CONTENEDOR COLLECT (US$)\", Valor: (login_extract.freight.per_container.USD_collect || 0).toLocaleString(\"es-AR\",{minimumFractionDigits:2}) });\n  pairs.push({ Dato: \"FLETE BL - COLLECT TOTAL (US$)\", Valor: (F?.totals?.USD?.collect || 0).toLocaleString(\"es-AR\",{minimumFractionDigits:2}) });\n  pairs.push({ Dato: \"FLETE BL - COLLECT TOTAL (R$)\", Valor: (F?.totals?.BRL?.collect || 0).toLocaleString(\"es-AR\",{minimumFractionDigits:2}) });\n\n  // Freight detalle por concepto (se muestran ambos USD/BRL tal cual)\n  for (const r of login_extract.freight.concepts){\n    if (!r.currency || r.amount == null) continue;\n    const cur = r.currency === \"BRL\" ? \"R$\" : \"US$\";\n    const amt = r.amount.toLocaleString(\"es-AR\",{minimumFractionDigits:2});\n    const tag = r.kind === \"PREPAID\" ? \"(PREPAID)\" : \"(COLLECT)\";\n    pairs.push({ Dato: `FLETE BL - ${r.concept.toUpperCase()} ${tag}`, Valor: `${cur} ${amt}` });\n  }\n\n  return { json: { ...j, login_extract, excel_pairs: pairs } };\n});\n"}, position: [1232, -320] },
  output: [{}]
});

const googleDriveBuscarPlanillaDeAduana_7 = node({
  type: "n8n-nodes-base.googleDrive",
  version: 3,
  config: { name: "Google Drive: Buscar “Planilla de Aduana”", parameters: {"resource": "fileFolder", "queryString": "={{$json.order_number || $json.orden_from_name}}", "limit": 1, "filter": {"folderId": {"__rl": true, "value": "1iPIfYz8ZLXFkju3FX1nc0gvZ-pZTOxzK", "mode": "list", "cachedResultName": "PLANILLA ADUANA - CONTENEDORES", "cachedResultUrl": "https://drive.google.com/drive/folders/1iPIfYz8ZLXFkju3FX1nc0gvZ-pZTOxzK"}}, "options": {"fields": ["mimeType", "id", "name", "webViewLink"]}}, position: [2208, -480], alwaysOutputData: true, credentials: { googleDriveOAuth2Api: newCredential("Google Drive account 2") } },
  output: [{}]
});

const googleDriveDownload_8 = node({
  type: "n8n-nodes-base.googleDrive",
  version: 3,
  config: { name: "Google Drive — Download", parameters: {"operation": "download", "fileId": {"__rl": true, "value": "={{$json.id}}", "mode": "id"}, "options": {}}, position: [2400, -480], alwaysOutputData: true, credentials: { googleDriveOAuth2Api: newCredential("Google Drive account 2") } },
  output: [{}]
});

const pdfExtractFromPdfAduana_9 = node({
  type: "n8n-nodes-base.extractFromFile",
  version: 1,
  config: { name: "PDF — Extract From PDF (Aduana)", parameters: {"operation": "pdf", "options": {"keepSource": "json"}}, position: [2576, -480], alwaysOutputData: true },
  output: [{}]
});

const pdfTextoBooking_10 = node({
  type: "n8n-nodes-base.extractFromFile",
  version: 1,
  config: { name: "PDF → Texto (Booking)", parameters: {"operation": "pdf", "options": {"keepSource": "json"}}, position: [2576, -288] },
  output: [{}]
});

const downloadBooking_11 = node({
  type: "n8n-nodes-base.googleDrive",
  version: 3,
  config: { name: "Download (Booking)", parameters: {"operation": "download", "fileId": {"__rl": true, "value": "={{$json.id}}", "mode": "id"}, "options": {}}, position: [2400, -288], alwaysOutputData: true, credentials: { googleDriveOAuth2Api: newCredential("Google Drive account 2") } },
  output: [{}]
});

const buscarBookingAdviceEnDrive_12 = node({
  type: "n8n-nodes-base.googleDrive",
  version: 3,
  config: { name: "Buscar Booking Advice en Drive", parameters: {"resource": "fileFolder", "queryString": "={{$json.order_number || $json.orden_from_name}}", "limit": 1, "filter": {"driveId": {"__rl": true, "value": "0AKuox28BE9ytUk9PVA", "mode": "list", "cachedResultName": "TEAM EXPORTACION  ", "cachedResultUrl": "https://drive.google.com/drive/folders/0AKuox28BE9ytUk9PVA"}, "folderId": {"__rl": true, "value": "1ALgZe9TS6u5lqWrhKraWxQ785G_tYI5V", "mode": "list", "cachedResultName": "BOOKING ADVICE - ZCB3", "cachedResultUrl": "https://drive.google.com/drive/folders/1ALgZe9TS6u5lqWrhKraWxQ785G_tYI5V"}, "whatToSearch": "files"}, "options": {"fields": ["id", "name", "webViewLink", "mimeType"]}}, position: [2208, -288], alwaysOutputData: true, credentials: { googleDriveOAuth2Api: newCredential("Google Drive account 2") } },
  output: [{}]
});

const setAduanaJoinKey_13 = node({
  type: "n8n-nodes-base.set",
  version: 3.4,
  config: { name: "Set Aduana: Join Key", parameters: {"assignments": {"assignments": [{"id": "19c617b9-6c8f-4cae-bc7b-4832490595ee", "name": "joinKey", "value": "={{ ($json.aduana_extract?.operacion || $json.order_number || $json.orden_from_name || '').replace(/[^0-9A-Za-z]/g,'') }}", "type": "string"}]}, "includeOtherFields": true, "options": {}}, position: [2960, -480] },
  output: [{}]
});

const setBlJoinKey_14 = node({
  type: "n8n-nodes-base.set",
  version: 3.4,
  config: { name: "Set BL: Join Key", parameters: {"assignments": {"assignments": [{"id": "84bc77cc-9484-4756-af6d-88926b62c79b", "name": "=joinKey", "value": "={{ \n  (() => {\n    const src =\n      $json.login_extract?.export_references?.[0] ??\n      $json.login_extract?.order_number ??\n      $json.order_number ??\n      \"\";\n\n    // Devuelve solo dígitos; elimina \\n, espacios, guiones, etc.\n    return String(src).replace(/\\D+/g, \"\");\n  })()\n}}", "type": "string"}]}, "includeOtherFields": true, "options": {}}, position: [2256, -688] },
  output: [{}]
});

const setBookingJoinKey_15 = node({
  type: "n8n-nodes-base.set",
  version: 3.4,
  config: { name: "Set Booking: Join Key", parameters: {"assignments": {"assignments": [{"id": "3cc8030b-c572-427e-9a1e-95bec0d52ca7", "name": "joinKey", "value": "={{\n  ($json.booking_extract?.order_number || $json.order_number || '')\n    .toString()\n    .replace(/[^0-9]/g,'')\n    .replace(/^0+/, '')\n}}", "type": "string"}]}, "includeOtherFields": true, "options": {}}, position: [2960, -288] },
  output: [{}]
});

const merge1BlAduana_16 = node({
  type: "n8n-nodes-base.merge",
  version: 3.2,
  config: { name: "Merge 1 (BL + Aduana)", parameters: {"mode": "combine", "fieldsToMatchString": "joinKey", "options": {}}, position: [3216, -672] },
  output: [{}]
});

const merge2AgregarBooking_17 = node({
  type: "n8n-nodes-base.merge",
  version: 3.2,
  config: { name: "Merge 2 (agregar Booking)", parameters: {"mode": "combine", "fieldsToMatchString": "joinKey", "options": {"clashHandling": {"values": {"resolveClash": "preferInput1"}}}}, position: [3392, -512] },
  output: [{}]
});

const setDestinatarios_18 = node({
  type: "n8n-nodes-base.set",
  version: 3.4,
  config: { name: "Set – Destinatarios", parameters: {"assignments": {"assignments": [{"id": "c1a2bf4b-2726-456b-b123-458b3718cf7b", "name": "email_to", "value": "jzenteno@ssbint.com.", "type": "string"}]}, "includeOtherFields": true, "options": {}}, position: [3792, -512] },
  output: [{}]
});

const sendAMessage_19 = node({
  type: "n8n-nodes-base.gmail",
  version: 2.1,
  config: { name: "Send a message", parameters: {"sendTo": "={{ Array.isArray($json.email_to) ? $json.email_to.join(', ') : String($json.email_to || '') }}\n", "subject": "={{$json.subject}}", "message": "={{$json.body_html}}", "options": {}}, position: [4160, -512], credentials: { gmailOAuth2: newCredential("Gmail account 3") } },
  output: [{}]
});

const codePlantillaHtml_20 = node({
  type: "n8n-nodes-base.code",
  version: 2,
  config: { name: "code  - plantilla HTML", parameters: {"jsCode": "// ============================\n//  Code – plantilla HTML (nodo antes de Gmail)\n// ============================\n\n// Helpers\nconst esc = (v) =>\n  String(v ?? '')\n    .replace(/&/g, '&amp;')\n    .replace(/</g, '&lt;')\n    .replace(/>/g, '&gt;')\n    .replace(/\"/g, '&quot;');\n\nconst escWithBreaks = (v) => esc(v).replace(/\\r?\\n/g, '<br/>');\n\n// Dejar celda en blanco si falta o si viene el literal \"SIN DATO\"/\"—\"\nconst clean = (v) => {\n  if (v === null || v === undefined) return '';\n  const s = String(v).trim();\n  if (!s) return '';\n  if (s.toUpperCase() === 'SIN DATO' || s === '—') return '';\n  return s;\n};\n\nconst money = (cur, n) => {\n  if (n === null || n === undefined || n === '' || isNaN(Number(n))) return '—';\n  const sym = cur === 'BRL' ? 'R$' : 'US$';\n  return `${sym} ${Number(n).toLocaleString('es-AR', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`;\n};\nconst moneyUSD = (n) => money('USD', n);\nconst moneyBRL = (n) => money('BRL', n);\n\nconst item = items[0];\nconst j = item.json || {};\n\n// Entradas principales\nconst bl  = j.login_extract || {};\nconst adu = j.aduana_extract || {};\nconst ba  = j.booking_extract || {};\nconst cmp = j.compare || {};\nconst cmpPairsAll = j.compare_excel_pairs || [];\nconst cmpEquip = j.compare_equipos || [];\n\n// Destinatarios (ya seteado como array en tu flujo)\nconst to = Array.isArray(j.email_to) ? j.email_to : ['jzenteno@ssbint.com'];\nconst cc = Array.isArray(j.email_cc) ? j.email_cc : []; // opcional\n\n// Campos para el asunto\nconst order   = j.order_number || bl.order_number || ba.order_number || j.joinKey || '';\nconst booking = j.booking_no || bl.booking_no || ba.booking_no || '';\nconst vessel  = bl.vessel || '';\nconst voyage  = bl.voyage || '';\n\n// Asunto\nconst subject =\n  `Orden ${order}` +\n  (booking ? ` | Booking ${booking}` : '') +\n  (vessel ? ` | ${vessel}` : '') +\n  (voyage ? ` ${voyage}` : '') +\n  ` — Comparación BL vs Aduana vs Booking`;\n\n// ===================================\n//  Tabla principal (comparaciones)\n// ===================================\nconst MULTILINE = new Set(['Shipper', 'Consignee Name', 'Notify Name']);\n\nconst HIDE_DATOS = new Set([\n  'THC ORIGEN',\n  'THC DESTINO',\n  'CANTIDAD DE CONTENEDORES',\n  'FLETE POR CONTENEDOR',\n  'FLETE TOTAL'\n]);\n\nconst cmpPairs = (cmpPairsAll || []).filter(r => !HIDE_DATOS.has(String(r.Dato || '').toUpperCase()));\n\nconst rowsMain = (cmpPairs || []).map(r => {\n  const estado = (r.Estado || '').toUpperCase();\n  const isOk   = estado === 'OK';\n  const dato   = String(r.Dato || '');\n\n  const render = (d, val) => {\n    const v = clean(val);\n    return MULTILINE.has(d) ? escWithBreaks(v) : esc(v);\n  };\n\n  return `\n    <tr class=\"${isOk ? 'ok' : 'rev'}\">\n      <td>${esc(dato)}</td>\n      <td>${render(dato, r.BL)}</td>\n      <td>${render(dato, r.Aduana)}</td>\n      <td>${render(dato, r['Booking Advice'])}</td>\n      <td class=\"estado\"><span class=\"tag ${isOk ? 'ok' : 'rev'}\">${esc(estado || '')}</span></td>\n      <td class=\"small\">${esc(clean(r.Nota))}</td>\n    </tr>`;\n}).join('');\n\n// ===================================\n//  Tabla por equipo (contenedores)\n// ===================================\nconst rowsEquip = (cmpEquip || []).map(e => {\n  const sealEstado = (e.seals?.estado || '').toUpperCase();\n  const netsEstado = (e.nets?.estado  || '').toUpperCase();\n  const grossEstado= (e.gross?.estado || '').toUpperCase();\n\n  const sealOk  = sealEstado  === 'OK';\n  const netsOk  = netsEstado  === 'OK';\n  const grossOk = grossEstado === 'OK';\n\n  return `\n    <tr>\n      <td>${esc(clean(e.container))}</td>\n      <td>${esc(clean(e.seals?.BL))}</td>\n      <td>${esc(clean(e.seals?.Aduana))}</td>\n      <td>${esc(clean(e.seals?.Booking))}</td>\n      <td class=\"estado\"><span class=\"tag ${sealOk ? 'ok' : 'rev'}\">${esc(sealEstado || '')}</span></td>\n      <td class=\"small\">${esc(clean(e.seals?.nota))}</td>\n      <td>${esc(clean(e.nets?.BL))}</td>\n      <td>${esc(clean(e.nets?.Aduana))}</td>\n      <td>${esc(clean(e.nets?.Booking))}</td>\n      <td class=\"estado\"><span class=\"tag ${netsOk ? 'ok' : 'rev'}\">${esc(netsEstado || '')}</span></td>\n      <td>${esc(clean(e.gross?.BL))}</td>\n      <td>${esc(clean(e.gross?.Aduana))}</td>\n      <td>${esc(clean(e.gross?.Booking))}</td>\n      <td class=\"estado\"><span class=\"tag ${grossOk ? 'ok' : 'rev'}\">${esc(grossEstado || '')}</span></td>\n    </tr>`;\n}).join('');\n\n// Resumen de contadores\nconst okCount  = cmp?.counters?.OK ?? 0;\nconst revCount = cmp?.counters?.REVISAR ?? 0;\nconst overall  = (cmp?.overall || 'REVISAR').toUpperCase();\n\n// Links (integrados en tarjetas)\nconst linkBL  = j.webViewLink || '';\nconst linkBA  = ba?.links?.webViewLink || '';\nconst linkAdu = adu?.source_link || j.aduana_link || ''; // preferir el que trae el parser de aduana\n\n// ===== Extractos – contenido (con \"Fuente: abrir\") =====\n\n// BL – campos destacados (si existen)\nconst blOriginals = clean(bl?.desc?.['DESC BL - ORIGINALS TO BE RELEASED AT'] || bl?.originals_to_be_released_at || '');\nconst blTypeMove  = clean(bl?.desc?.['DESC BL - TYPE OF MOVE'] || bl?.type_of_move || '');\n\nconst blBrief = `\n  <ul>\n    <li><b>BL/SWB:</b> ${esc(clean(bl.bl_no))}</li>\n    <li><b>Booking:</b> ${esc(clean(booking))}</li>\n    <li><b>Vessel/Voyage:</b> ${esc(clean(vessel))} ${esc(clean(voyage))}</li>\n    <li><b>POL → POD:</b> ${esc(clean(bl.pol))} → ${esc(clean(bl.pod))}</li>\n    <li><b>Consignee:</b> ${esc(clean((bl.consignee || '').split('\\\\n')[0]))}</li>\n    <li><b>Notify:</b> ${esc(clean((bl.notify || '').split('\\\\n')[0]))}</li>\n    ${blOriginals ? `<li class=\"hl\"><b>ORIGINALS TO BE RELEASED AT:</b> ${esc(blOriginals)}</li>` : ''}\n    ${blTypeMove  ? `<li class=\"hl\"><b>TYPE OF MOVE:</b> ${esc(blTypeMove)}</li>` : ''}\n    <li><b>Totales:</b> Net ${esc(clean(bl.desc?.['DESC BL - PESO NETO TOTAL (KG)']))} kg · Gross ${esc(clean(bl.desc?.['DESC BL - PESO BRUTO TOTAL (KG)']))} kg · Bultos ${esc(clean(bl.desc?.['DESC BL - CANTIDAD DE BOLSAS']))}</li>\n    ${linkBL ? `<li><b>Fuente:</b> <a href=\"${esc(linkBL)}\">abrir</a></li>` : ''}\n  </ul>`;\n\nconst baBrief = `\n  <ul>\n    <li><b>Order:</b> ${esc(clean(ba.order_number))}</li>\n    <li><b>POL → POD:</b> ${esc(clean(ba.pol))} → ${esc(clean(ba.pod))}</li>\n    <li><b>Incoterm:</b> ${esc(clean(ba.incoterm || ba.terms_of_delivery))} ${ba.incoterm_place ? '· ' + esc(clean(ba.incoterm_place)) : ''}</li>\n    <li><b>Notify (BA):</b> ${esc(clean(ba.notify?.name))} ${ba.notify?.tax_id ? '· CNPJ ' + esc(clean(ba.notify.tax_id)) : ''} ${ba.notify?.email ? '· ' + esc(clean(ba.notify.email)) : ''}</li>\n    <li><b>Fechas clave:</b> Doc ${esc(clean(ba.dates?.document_date))} · Cutoff ${esc(clean(ba.dates?.cutoff_origin))} · ETD ${esc(clean(ba.dates?.etd_pol))} · ETA ${esc(clean(ba.dates?.eta_destination))}</li>\n    ${linkBA ? `<li><b>Fuente:</b> <a href=\"${esc(linkBA)}\">abrir</a></li>` : ''}\n  </ul>`;\n\nconst aduBrief = `\n  <ul>\n    <li><b>Operación:</b> ${esc(clean(adu.operacion))}</li>\n    <li><b>Buque:</b> ${esc(clean(adu.buque))}</li>\n    <li><b>Destino:</b> ${esc(clean(adu.destino))}</li>\n    <li><b>PE:</b> ${esc(clean(adu.pe))}</li>\n    <li><b>Totales:</b> Net ${esc(clean(adu.totales?.net_kg || adu.totals?.neto))} kg · Gross ${esc(clean(adu.totales?.gross_kg || adu.totals?.bruto))} kg · Bultos ${esc(clean(adu.totales?.bultos || adu.totals?.bultos))}</li>\n    ${linkAdu ? `<li><b>Fuente:</b> <a href=\"${esc(linkAdu)}\">abrir</a></li>` : ''}\n  </ul>`;\n\n// =========================\n//  Tabla de tarifa BL (estilo BL) + Tarifa por contenedor con highlight\n// =========================\nconst charges = Array.isArray(bl?.freight?.concepts) ? bl.freight.concepts : [];\nconst totalsUSD = bl?.freight?.totals?.USD || { prepaid: 0, collect: 0 };\nconst totalsBRL = bl?.freight?.totals?.BRL || { prepaid: 0, collect: 0 };\nconst contCount = bl?.freight?.containers_for_calc ?? (Array.isArray(bl?.equipos) ? bl.equipos.length : 0);\n\n// Determinar OF kind desde el concepto \"Ocean Freight\"\nlet ofKind = null; // 'PREPAID' | 'COLLECT' | null\nfor (const c of charges) {\n  const name = String(c?.concept || '').toUpperCase();\n  const k = String(c?.kind || '').toUpperCase();\n  if (name.includes('OCEAN') && name.includes('FREIGHT') && (k === 'PREPAID' || k === 'COLLECT')) { ofKind = k; break; }\n}\n\n// Cálculo “tarifa por contenedor” en USD (distribuye por cantidad de contenedores)\nconst perPrepaidUSD = contCount ? (Number(totalsUSD.prepaid || 0) / contCount) : 0;\nconst perCollectUSD = contCount ? (Number(totalsUSD.collect || 0) / contCount) : 0;\n\n// Render de filas de cargos\nconst rowsCharges = charges.map(c => {\n  const concept = esc(c.concept || '');\n  const qty = contCount ? String(contCount) : '';\n  const rate = (c.rate != null) ? money(c.rate_currency === 'BRL' ? 'BRL' : 'USD', c.rate) : '—';\n  const prepaid = (String(c.kind || '').toUpperCase() === 'PREPAID') ? money(c.currency || 'USD', c.amount) : '—';\n  const collect = (String(c.kind || '').toUpperCase() === 'COLLECT') ? money(c.currency || 'USD', c.amount) : '—';\n  return `\n    <tr>\n      <td>${concept}</td>\n      <td class=\"right\">${esc(qty)}</td>\n      <td class=\"right\">${rate}</td>\n      <td class=\"right\">${prepaid}</td>\n      <td class=\"right\">${collect}</td>\n    </tr>`;\n}).join('');\n\n// Fila Total Reais / Total USD\nconst rowTotals = `\n  <tr class=\"tot\">\n    <td><b>TOTAL REAIS</b></td>\n    <td></td>\n    <td class=\"muted\">REALES</td>\n    <td class=\"right\">${moneyBRL(totalsBRL.prepaid)}</td>\n    <td class=\"right\">${moneyBRL(totalsBRL.collect)}</td>\n  </tr>\n  <tr class=\"tot\">\n    <td><b>TOTAL USD</b></td>\n    <td></td>\n    <td class=\"muted\">DOLARES</td>\n    <td class=\"right\">${moneyUSD(totalsUSD.prepaid)}</td>\n    <td class=\"right\">${moneyUSD(totalsUSD.collect)}</td>\n  </tr>`;\n\n// Fila Tarifa por contenedor (resaltando según OF kind)\nconst rowPerContainer = `\n  <tr class=\"tot\">\n    <td><b>TARIFA POR CONTENEDOR</b></td>\n    <td></td>\n    <td></td>\n    <td class=\"right ${ofKind === 'PREPAID' ? 'focus' : ''}\">${ofKind === 'PREPAID' ? moneyUSD(perPrepaidUSD) : '—'}</td>\n    <td class=\"right ${ofKind === 'COLLECT' ? 'focus' : ''}\">${ofKind === 'COLLECT' ? moneyUSD(perCollectUSD) : '—'}</td>\n  </tr>`;\n\n// =========================\n//  HTML (tarjetas + comparativo + equipos + tabla tarifa BL)\n// =========================\nconst html = `\n  <style>\n    body { font-family: Arial, sans-serif; font-size: 13px; color:#222; }\n    h2 { margin: 16px 0 8px; font-size:16px; }\n    table{ border-collapse: collapse; width:100%; font-family: Arial, sans-serif; font-size:12px; }\n    th,td{ border:1px solid #ddd; padding:6px 8px; vertical-align: top; }\n    th{ background:#f5f5f5; text-align:left; }\n    td.right { text-align:right; }\n    tr.ok td.estado{ color:#0a7a28; font-weight:700; }\n    tr.rev{ background:#fff7e6; }\n    tr.rev td.estado{ color:#b55300; font-weight:700; }\n    .tag{ display:inline-block; padding:2px 6px; border-radius:4px; font-size:11px; font-weight:700; }\n    .tag.ok{ background:#e6f4ea; color:#137333; }\n    .tag.rev{ background:#fff4e5; color:#b06000; }\n    .small{ color:#666; font-size:11px; }\n    .chips{ margin:8px 0; }\n    .chip{ display:inline-block; margin-right:8px; padding:2px 8px; border-radius:12px; background:#eef2f7; font-size:12px; }\n    .hl { background:#fff7cc; padding:2px 4px; border-radius:3px; } /* highlight para BL extract */\n\n    /* Tarjetas en 3 columnas */\n    .cards3{ width:100%; border-collapse:separate; border-spacing:16px 0; table-layout:fixed; }\n    .cards3 td.card{ vertical-align:top; border:1px solid #ddd; border-radius:6px; padding:8px; width:33.33%; }\n    .cards3 h3{ margin:0 0 8px; font-size:14px; }\n\n    .m16{ margin-top:16px; }\n    .m24{ margin-top:24px; }\n\n    /* Tabla de cargos estilo BL */\n    .charges { width:100%; border-collapse:collapse; }\n    .charges th, .charges td{ border:1px solid #ddd; padding:8px; }\n    .charges th{ background:#f5f5f5; }\n    .charges tr.tot td{ background:#fafafa; font-weight:600; }\n    .charges td.muted{ color:#666; font-style:italic; }\n    .focus{ background:#fff3cd !important; } /* celda destacada (OF) */\n  </style>\n\n  <div class=\"chips\">\n    <span class=\"chip\"><b>Orden:</b> ${esc(clean(order))}</span>\n    ${booking ? `<span class=\"chip\"><b>Booking:</b> ${esc(clean(booking))}</span>` : ''}\n    ${vessel || voyage ? `<span class=\"chip\"><b>Vessel/Voyage:</b> ${esc(clean(vessel))} ${esc(clean(voyage))}</span>` : ''}\n    <span class=\"chip\"><b>Resultado:</b> <span class=\"tag ${overall === 'OK' ? 'ok' : 'rev'}\">${esc(overall)}</span></span>\n    <span class=\"chip\"><b>OK:</b> ${esc(okCount)}</span>\n    <span class=\"chip\"><b>REVISAR:</b> ${esc(revCount)}</span>\n    ${ofKind ? `<span class=\"chip\"><b>O/F:</b> ${esc(ofKind)}</span>` : ''}\n  </div>\n\n  <h2>Extracto de cada documento</h2>\n  <table class=\"cards3\">\n    <tr>\n      <td class=\"card\">\n        <h3>BL draft</h3>\n        ${blBrief}\n      </td>\n      <td class=\"card\">\n        <h3>Planilla de Aduana</h3>\n        ${aduBrief}\n      </td>\n      <td class=\"card\">\n        <h3>Booking Advice</h3>\n        ${baBrief}\n      </td>\n    </tr>\n  </table>\n\n  <h2 class=\"m16\">Comparación de campos clave</h2>\n  <table>\n    <thead>\n      <tr>\n        <th>Dato</th>\n        <th>BL</th>\n        <th>Aduana</th>\n        <th>Booking Advice</th>\n        <th>Estado</th>\n        <th>Nota</th>\n      </tr>\n    </thead>\n    <tbody>\n      ${rowsMain || `<tr><td colspan=\"6\" class=\"small\"></td></tr>`}\n    </tbody>\n  </table>\n\n  <h2 class=\"m16\">Detalle por contenedor</h2>\n  <table>\n    <thead>\n      <tr>\n        <th>Container</th>\n        <th>Seal (BL)</th>\n        <th>Seal (Aduana)</th>\n        <th>Seal (BA)</th>\n        <th>Estado Seal</th>\n        <th>Nota</th>\n        <th>Net (BL)</th>\n        <th>Net (Aduana)</th>\n        <th>Net (BA)</th>\n        <th>Estado Net</th>\n        <th>Gross (BL)</th>\n        <th>Gross (Aduana)</th>\n        <th>Gross (BA)</th>\n        <th>Estado Gross</th>\n      </tr>\n    </thead>\n    <tbody>\n      ${rowsEquip || `<tr><td colspan=\"14\" class=\"small\"></td></tr>`}\n    </tbody>\n  </table>\n\n  <!-- Tabla de tarifas (BL) -->\n  <div class=\"m24\">\n    <h2>Detalle de tarifa (BL)</h2>\n    <table class=\"charges\">\n      <thead>\n        <tr>\n          <th>FREIGHT CHARGES</th>\n          <th>QUANTITY</th>\n          <th>RATE</th>\n          <th>PREPAID</th>\n          <th>COLLECT</th>\n        </tr>\n      </thead>\n      <tbody>\n        ${rowsCharges}\n        ${rowTotals}\n        ${rowPerContainer}\n      </tbody>\n    </table>\n  </div>\n\n  <p style=\"margin-top:16px\">\n    Por favor <b>revisar</b> las filas marcadas como <span class=\"tag rev\">REVISAR</span> y confirmar <b>OK</b> o responder con observaciones.\n  </p>\n`;\n\n// Texto plano (fallback)\nconst bodyText =\n`Orden ${order} ${booking ? `| Booking ${booking}` : ''} ${vessel ? `| ${vessel} ${voyage}` : ''}\nResultado: ${overall} (OK: ${okCount} | REVISAR: ${revCount})\n\nBL: ${j.webViewLink || ''}\nBA: ${ba?.links?.webViewLink || ''}\nAduana: ${linkAdu || ''}`;\n\n// Devolvemos el item para el nodo Gmail\nreturn [{\n  json: {\n    email_to: to,\n    email_cc: cc,\n    subject,\n    body_html: html,\n    body_text: bodyText\n  }\n}];\n"}, position: [3984, -512] },
  output: [{}]
});

const comparadorBlVsAduanaVsBooking_21 = node({
  type: "n8n-nodes-base.code",
  version: 2,
  config: { name: "COMPARADOR - BL vs Aduana vs Booking", parameters: {"mode": "runOnceForEachItem", "jsCode": "'use strict';\n\n/* =========================\n   Utilidades\n   ========================= */\nconst upper = (s) => (s == null ? '' : String(s).trim().toUpperCase());\nconst cleanDigits = (s) => (s == null ? '' : String(s).replace(/\\D+/g, ''));\nconst joinUniq = (arr, sep=', ') => [...new Set((arr || []).filter(Boolean))].join(sep);\n\n// Convierte strings numéricos con , . miles/decimales → Number\nconst toNum = (x) => {\n  if (x == null) return null;\n  const str0 = String(x).trim();\n  if (!str0) return null;\n\n  let s = str0.replace(/[^\\d.,\\-]/g, '');\n  const lastComma = s.lastIndexOf(',');\n  const lastDot   = s.lastIndexOf('.');\n\n  const THOUSANDS_COMMA = /^\\d{1,3}(?:,\\d{3})+$/;\n  const THOUSANDS_DOT   = /^\\d{1,3}(?:\\.\\d{3})+$/;\n\n  if (lastComma > -1 && lastDot > -1) {\n    if (lastComma > lastDot) { s = s.replace(/\\./g, '').replace(/,/g, '.'); }\n    else { s = s.replace(/,/g, ''); }\n  } else if (lastComma > -1) {\n    if (THOUSANDS_COMMA.test(s)) s = s.replace(/,/g, '');\n    else s = s.replace(/,/g, '.');\n  } else if (THOUSANDS_DOT.test(s)) {\n    s = s.replace(/\\./g, '');\n  }\n  const n = Number(s);\n  return Number.isFinite(n) ? n : null;\n};\n\n// Normalización de países (idiomas/variantes)\nconst stripDiacritics = (s) => s.normalize('NFKD').replace(/[\\u0300-\\u036f]/g, '');\nconst canonCountry = (s) => {\n  if (!s) return '';\n  const t = upper(stripDiacritics(String(s))).replace(/[^\\w\\s]/g,'').replace(/\\s+/g,' ').trim();\n  const map = {\n    'ARGENTINA': 'ARGENTINA',\n    'BRASIL': 'BRAZIL',\n    'BRAZIL': 'BRAZIL',\n    'ESTADOS UNIDOS': 'UNITED STATES',\n    'EEUU': 'UNITED STATES',\n    'EUA': 'UNITED STATES',\n    'UNITED STATES OF AMERICA': 'UNITED STATES',\n    'REINO UNIDO': 'UNITED KINGDOM',\n    'UK': 'UNITED KINGDOM',\n    'UNITED KINGDOM': 'UNITED KINGDOM',\n    'VIET NAM': 'VIETNAM',\n  };\n  return map[t] || t;\n};\nconst eqCountry = (a,b) => a && b && canonCountry(a) === canonCountry(b);\n\n// Emails case-insensitive\nconst eqEmail = (a,b) => upper(a) === upper(b);\n\n// Bloques multilínea (Shipper/Consignee/Notify)\nconst buildBlock = (val) => {\n  if (val == null) return '';\n  if (typeof val === 'string') return val.trim();\n\n  const lines = [];\n  if (val.name) lines.push(String(val.name).trim());\n\n  if (Array.isArray(val.address_lines) && val.address_lines.length) {\n    for (const ln of val.address_lines) lines.push(String(ln).trim());\n  } else if (val.address_str) {\n    const parts = String(val.address_str).split(/\\s*,\\s*/);\n    if (parts.length > 1) lines.push(...parts);\n    else lines.push(String(val.address_str).trim());\n  }\n\n  if (val.tax_id) lines.push(`CNPJ ${cleanDigits(val.tax_id)}`);\n  if (val.email)  lines.push(String(val.email).trim());\n  return lines.filter(Boolean).join('\\n');\n};\n\nconst listContainers = (arr) => (Array.isArray(arr) ? arr.map(o => o && o.container).filter(Boolean) : []);\n\n/* =========================\n   Ocean Freight → bucket Incoterm\n   ========================= */\nconst getOceanFreightKindFromBL = (bl) => {\n  const concepts = (bl && bl.freight && Array.isArray(bl.freight.concepts)) ? bl.freight.concepts : [];\n  for (const c of concepts) {\n    const name = upper(c && c.concept);\n    if (name.includes('OCEAN') && name.includes('FREIGHT')) {\n      const k = upper(c && c.kind);\n      return (k === 'PREPAID' || k === 'COLLECT') ? k : null;\n    }\n  }\n  return null;\n};\nconst expectedIncotermBucketByFreight = (kind) => {\n  if (kind === 'PREPAID') return {label: 'C/D', allowed: new Set(['C','D'])};\n  if (kind === 'COLLECT') return {label: 'E/F', allowed: new Set(['E','F'])};\n  return null;\n};\n\n/* =========================\n   Comparativa por equipos (objeto resumido)\n   ========================= */\nfunction buildCompareEquipos(bl, ba, adu) {\n  const map = {};\n  const push = (k, src, obj) => {\n    if (!k) return;\n    map[k] = map[k] || { container: k };\n    map[k][src] = obj;\n  };\n\n  (Array.isArray(bl?.equipos) ? bl.equipos : []).forEach(e => {\n    push(e.container, 'BL', {\n      seal: e.seal || e.seal_or_precinto || '',\n      net: toNum(e.nw || e.net_kg || e.nw_kg),\n      gross: toNum(e.gw || e.gross_kg || e.gw_kg),\n    });\n  });\n  (Array.isArray(ba?.equipos) ? ba.equipos : []).forEach(e => {\n    push(e.container, 'BA', {\n      seal: e.seal || e.seal_or_precinto || '',\n      net: toNum(e.net_kg || e.nw),\n      gross: toNum(e.gross_kg || e.gw),\n    });\n  });\n  (Array.isArray(adu?.contenedores) ? adu.contenedores : []).forEach(e => {\n    push(e.container, 'Adu', {\n      seal: e.precinto || e.seal || '',\n      net: toNum(e.neto),\n      gross: toNum(e.bruto),\n    });\n  });\n\n  const out = [];\n  for (const k of Object.keys(map).sort()) {\n    const it = map[k];\n\n    const sealBL = it.BL?.seal || '';\n    const sealAD = it.Adu?.seal || '';\n    const sealBA = it.BA?.seal || '';\n    const sealNote = (sealBA && ((upper(sealBA)!==upper(sealBL)) || (upper(sealBA)!==upper(sealAD)))) ? 'Precinto BA difiere de BL/Aduana' : '';\n\n    const netsEqual = (it.BL?.net != null && it.Adu?.net != null && it.BA?.net != null && it.BL.net === it.Adu.net && it.BL.net === it.BA.net);\n    const grossEqual = (it.BL?.gross != null && it.Adu?.gross != null && it.BA?.gross != null && it.BL.gross === it.Adu.gross && it.BL.gross === it.BA.gross);\n\n    out.push({\n      label: `Equipo — ${k}`,\n      container: k,\n      seals: { BL: sealBL, Aduana: sealAD, Booking: sealBA, estado: 'OK', nota: sealNote },\n      nets:   { BL: it.BL?.net ?? '', Aduana: it.Adu?.net ?? '', Booking: it.BA?.net ?? '', estado: netsEqual ? 'OK' : 'REVISAR' },\n      gross:  { BL: it.BL?.gross ?? '', Aduana: it.Adu?.gross ?? '', Booking: it.BA?.gross ?? '', estado: grossEqual ? 'OK' : 'REVISAR' },\n    });\n  }\n  return out;\n}\n\n/* =========================\n   Comparativa general (tabla)\n   ========================= */\nfunction buildComparison(doc) {\n  const bl = doc.login_extract || {};\n  const ba = doc.booking_extract || {};\n  const adu = doc.aduana_extract || {};\n\n  const rows = [];\n  const counters = { OK: 0, REVISAR: 0 };\n  const bump = (st) => { if (st === 'REVISAR') counters.REVISAR++; else counters.OK++; };\n  const addRow = (Dato, BL, Aduana, BA, Estado='OK', Nota='') => {\n    rows.push({ Dato, BL: (BL ?? 'SIN DATO'), Aduana: (Aduana ?? 'SIN DATO'), 'Booking Advice': (BA ?? 'SIN DATO'), Estado, Nota });\n  };\n\n  // Order Number\n  const orderBL = (Array.isArray(bl.export_references) && bl.export_references[0]) || bl.order_number || doc.order_number || '';\n  const orderBA = ba.order_number || doc.order_number || '';\n  const orderAD = adu.operacion || '';\n  const stOrder = (orderBL && orderBA && orderBL === orderBA) ? 'OK' : 'REVISAR';\n  addRow('Order Number', orderBL, orderAD, orderBA, stOrder, '');\n  bump(stOrder);\n\n  // Booking Number\n  const bookingBL = bl.booking_no || doc.booking_no || '';\n  const bookingBA = ba.booking_no || ba.booking || doc.booking_no || '';\n  const stBook = (bookingBL && bookingBA && bookingBL === bookingBA) ? 'OK' : 'REVISAR';\n  addRow('Booking Number', bookingBL, '', bookingBA, stBook, '');\n  bump(stBook);\n\n  // BL Number (informativo)\n  addRow('BL Number', bl.bl_no || bl.bl || '', '', '', 'OK', 'Solo BL (informativo)');\n  bump('OK');\n\n  // Incoterm validado con Ocean Freight del BL\n  const baIncoterm = upper(ba.incoterm).slice(0,3);\n  const ofKind = getOceanFreightKindFromBL(bl); // PREPAID | COLLECT | null\n  const bucket = expectedIncotermBucketByFreight(ofKind);\n  let stInc = 'OK', notaInc = '';\n  if (bucket && baIncoterm) {\n    const first = baIncoterm.charAt(0);\n    if (!bucket.allowed.has(first)) {\n      stInc = 'REVISAR';\n      notaInc = `Ocean Freight BL=${ofKind} ⇒ esperado ${bucket.label}; BA tiene ${baIncoterm}`;\n    } else {\n      notaInc = `Validación por Ocean Freight (BL=${ofKind})`;\n    }\n  } else if (!baIncoterm && bucket) {\n    stInc = 'REVISAR';\n    notaInc = `BA sin Incoterm y BL indica ${ofKind} (esperado ${bucket.label})`;\n  }\n  const blIncotermShown = bucket ? `O/F ${ofKind} ⇒ ${bucket.label}` : (upper(bl.incoterm).slice(0,3) || '');\n  addRow('Incoterm', blIncotermShown, '', baIncoterm, stInc, notaInc);\n  bump(stInc);\n\n  // Incoterm Place\n  const blIncotermReal = upper(bl.incoterm).slice(0,3);\n  const blIncPlace = blIncotermReal ? upper(bl.incoterm_place || '') : '';\n  const baIncPlace = upper(ba.incoterm_place || ba.incoterm_place_hint || '');\n  let stPlc = 'OK', notaPlc = '';\n  if (blIncotermReal) {\n    if (blIncPlace && baIncPlace && blIncPlace !== baIncPlace) {\n      stPlc = 'REVISAR'; notaPlc = 'Incoterm Place difiere BL vs BA';\n    }\n  } else {\n    notaPlc = 'BL sin Incoterm: Place no se compara';\n  }\n  addRow('Incoterm Place', blIncPlace, '', baIncPlace, stPlc, notaPlc);\n  bump(stPlc);\n\n  // Shipper (informativo)\n  addRow('Shipper', buildBlock(bl.shipper || ''), '', '', 'OK', 'Solo BL (informativo)');\n  bump('OK');\n\n  // Vessel\n  const vesselBL = bl.vessel || '';\n  const vesselAD = adu.buque || '';\n  const stVessel = (vesselBL && vesselAD) ? (upper(vesselBL) === upper(vesselAD) ? 'OK' : 'REVISAR') : 'OK';\n  addRow('Vessel', vesselBL, vesselAD, '', stVessel, '');\n  bump(stVessel);\n\n  // Voyage (informativo)\n  addRow('Voyage', bl.voyage || '', '', '', 'OK', '');\n  bump('OK');\n\n  // POL / POD\n  addRow('POL', bl.pol || '', '', ba.pol || ba.POL || '', 'OK', '');\n  bump('OK');\n  addRow('POD', bl.pod || '', '', ba.pod || ba.POD || '', 'OK', '');\n  bump('OK');\n\n  // Destino país (normalizado)\n  const aduPais = adu.destino || '';\n  const baPais = ba.destino_pais || ba.country || '';\n  const stPais = (aduPais && baPais && eqCountry(aduPais, baPais)) ? 'OK' : ((aduPais || baPais) ? 'REVISAR' : 'OK');\n  addRow('Destino (País)', '', aduPais, baPais, stPais, stPais==='REVISAR' ? 'Aduana vs Booking (normalizado)' : '');\n  bump(stPais);\n\n  // Consignee\n  const consigBL = bl.consignee || '';\n  const consigBA = buildBlock(ba.consignee || {});\n  addRow('Consignee Name', buildBlock(consigBL), '', consigBA, 'OK', '');\n  bump('OK');\n\n  // Consignee Tax ID (CNPJ)\n  const taxBL = cleanDigits((/CNPJ\\s*([\\d./-]+)/i.exec(String(consigBL))||[])[1] || bl.consignee_tax || '');\n  const taxBA = cleanDigits(ba?.consignee?.tax_id || '');\n  const stTax = (taxBL && taxBA && taxBL === taxBA) ? 'OK' : ((taxBL || taxBA) ? 'REVISAR' : 'OK');\n  addRow('Consignee Tax ID (CNPJ)', taxBL, '', taxBA, stTax, '');\n  bump(stTax);\n\n  // Notify\n  const notifyBL = bl.notify || '';\n  const notifyBA = buildBlock(ba.notify || {});\n  addRow('Notify Name', buildBlock(notifyBL), '', notifyBA, 'OK', '');\n  bump('OK');\n\n  const notifyTaxBL = cleanDigits((/(TAX\\s*ID|CNPJ)\\s*[: ]*\\s*([0-9./-]+)/i.exec(String(notifyBL))||[])[2] || '');\n  const notifyTaxBA = cleanDigits(ba?.notify?.tax_id || '');\n  const stNotifyTax = (notifyTaxBL && notifyTaxBA && notifyTaxBL === notifyTaxBA) ? 'OK' : ((notifyTaxBL || notifyTaxBA) ? 'REVISAR' : 'OK');\n  addRow('Notify Tax ID', notifyTaxBL, '', notifyTaxBA, stNotifyTax, '');\n  bump(stNotifyTax);\n\n  const notifyEmailBL = (String(notifyBL).match(/[A-Z0-9._%+-]+@[A-Z0-9.-]+\\.[A-Z]{2,}/ig)||[])[0] || '';\n  const notifyEmailBA = ba?.notify?.email || '';\n  const stEmail = (notifyEmailBL && notifyEmailBA && eqEmail(notifyEmailBL, notifyEmailBA)) ? 'OK' : ((notifyEmailBL || notifyEmailBA) ? 'REVISAR' : 'OK');\n  addRow('Notify Email', notifyEmailBL || '', '', notifyEmailBA || '', stEmail, stEmail==='REVISAR' ? 'Email notify difiere o falta' : '');\n  bump(stEmail);\n\n  // ===== Producto (descripción completa) =====\n  const prodBLFull = bl?.desc?.['DESC BL - GOODS (DESCRIPCIÓN CRUDA)'] || '';\n  const prodBAFull = ba?.producto?.cadena || '';\n  const aduProds    = Array.isArray(adu?.contenedores) ? joinUniq(adu.contenedores.map(c => c?.producto).filter(Boolean), ' | ') : '';\n\n  let stProd = 'OK', notaProd = '';\n  if (prodBLFull && prodBAFull) {\n    stProd = (upper(prodBLFull) === upper(prodBAFull)) ? 'OK' : 'REVISAR';\n    if (stProd === 'REVISAR') notaProd = 'BL vs BA (insensible a mayúsculas)';\n  } else if (prodBLFull || prodBAFull) {\n    stProd = 'REVISAR';\n    notaProd = 'Falta en BL o BA';\n  }\n  addRow('Producto (descripción completa)', prodBLFull, aduProds, prodBAFull, stProd, notaProd);\n  bump(stProd);\n\n  // Grado / Embalaje\n  addRow('Grado', bl?.desc?.['DESC BL - GRADE / CALIDAD'] || '', '', ba?.producto?.grado || '', 'OK', '');\n  bump('OK');\n\n  addRow('Embalaje', bl?.desc?.['DESC BL - TIPO DE EMBALAJE'] || '', '', ba?.producto?.embalaje || '', 'OK', '');\n  bump('OK');\n\n  // Totales\n  const bultosBL = String(\n    bl?.desc?.['DESC BL - CANTIDAD DE BOLSAS'] ??\n    bl?.desc?.['DESC BL - CANTIDAD DE PALLETS'] ??\n    bl?.desc?.['DESC BL - CANTIDAD DE CONTENEDORES'] ??\n    ''\n  ).trim();\n  const bultosAD = adu?.totals?.bultos ?? '';\n  const bultosBA = ba?.totales?.piece_count ?? '';\n  const stBultos = (String(bultosAD) === String(bultosBA)) ? 'OK' : 'REVISAR';\n  addRow('Bultos (Total)', bultosBL, bultosAD, bultosBA, stBultos, '');\n  bump(stBultos);\n\n  addRow('Peso Neto Total (KG)', String(bl?.desc?.['DESC BL - PESO NETO TOTAL (KG)'] || ''), adu?.totals?.neto ?? '', ba?.totales?.net_kg ?? '', 'OK', '');\n  bump('OK');\n  addRow('Peso Bruto Total (KG)', String(bl?.desc?.['DESC BL - PESO BRUTO TOTAL (KG)'] || ''), adu?.totals?.bruto ?? '', ba?.totales?.gross_kg ?? '', 'OK', '');\n  bump('OK');\n\n  // HS/NCM\n  addRow('HS/NCM (4 dígitos)', bl?.desc?.['DESC BL - NCM'] || '', '', ba?.hs?.import || ba?.ncm_export || '', 'OK', '');\n  bump('OK');\n\n  // PE\n  addRow('Permiso de Embarque (PE)', bl?.desc?.['DESC BL - PE (PERMISO DE EMBARQUE)'] || '', adu?.ddt || '', '', 'OK', '');\n  bump('OK');\n\n  // Contenedores listado\n  const contBL = listContainers(bl?.equipos);\n  const contAD = listContainers(adu?.contenedores);\n  const contBA = listContainers(ba?.equipos);\n  addRow('Contenedores (lista)', joinUniq(contBL), joinUniq(contAD), joinUniq(contBA), 'OK', '');\n  bump('OK');\n\n  // BL Freight (informativo)\n  const blCharges = bl?.freight?.totals || {};\n  const blThcOr = blCharges?.USD?.prepaid ?? (bl?.desc_flete?.thc_origen ?? '');\n  const blThcDe = blCharges?.BRL?.collect ?? (bl?.desc_flete?.thc_destino ?? '');\n  addRow('THC ORIGEN', (toNum(blThcOr) ?? ''), '', '', 'OK', 'Solo BL (informativo)'); bump('OK');\n  addRow('THC DESTINO', (toNum(blThcDe) ?? ''), '', '', 'OK', 'Solo BL (informativo)'); bump('OK');\n  addRow('CANTIDAD DE CONTENEDORES', ((bl?.freight?.containers_for_calc ?? contBL.length) || ''), '', '', 'OK', 'Solo BL (informativo)'); bump('OK');\n  addRow('FLETE POR CONTENEDOR', (bl?.freight?.per_container?.USD ?? 0), '', '', 'OK', 'Solo BL (informativo)'); bump('OK');\n  const blFleteTotal = (toNum(bl?.freight?.totals?.USD?.collect) || 0) + (toNum(bl?.freight?.totals?.USD?.prepaid) || 0);\n  addRow('FLETE TOTAL', blFleteTotal || '', '', '', 'OK', 'Solo BL (informativo)'); bump('OK');\n\n  // ---- Equipos en tabla (con pesos corregidos) ----\n  const equipoMap = {};\n  const put = (k, src, obj) => {\n    if (!k) return;\n    equipoMap[k] = equipoMap[k] || { container: k };\n    equipoMap[k][src] = obj;\n  };\n  (Array.isArray(bl?.equipos) ? bl.equipos : []).forEach(e => put(e.container, 'BL', {\n    seal: e.seal || e.seal_or_precinto || '',\n    net: toNum(e.nw || e.net_kg || e.nw_kg),\n    gross: toNum(e.gw || e.gross_kg || e.gw_kg),\n  }));\n  (Array.isArray(ba?.equipos) ? ba.equipos : []).forEach(e => put(e.container, 'BA', {\n    seal: e.seal || e.seal_or_precinto || '',\n    net: toNum(e.net_kg || e.nw),\n    gross: toNum(e.gross_kg || e.gw),\n  }));\n  (Array.isArray(adu?.contenedores) ? adu.contenedores : []).forEach(e => put(e.container, 'Adu', {\n    seal: e.precinto || e.seal || '',\n    net: toNum(e.neto),\n    gross: toNum(e.bruto),\n  }));\n\n  const equiposRows = [];\n  for (const k of Object.keys(equipoMap).sort()) {\n    const it = equipoMap[k];\n    const label = `Equipo — ${k}`;\n\n    const sealBL = it.BL?.seal || '';\n    const sealAD = it.Adu?.seal || '';\n    const sealBA = it.BA?.seal || '';\n    const sealNote = (sealBA && ((upper(sealBA)!==upper(sealBL)) || (upper(sealBA)!==upper(sealAD)))) ? 'Precinto BA difiere de BL/Aduana' : '';\n\n    const nBL = it.BL?.net ?? '';\n    const nAD = it.Adu?.net ?? '';\n    const nBA = it.BA?.net ?? '';\n    const stN = (nBL && nAD && nBA && nBL===nAD && nBL===nBA) ? 'OK' : ((nAD && nBA && nAD===nBA) ? 'OK' : 'REVISAR');\n\n    const gBL = it.BL?.gross ?? '';\n    const gAD = it.Adu?.gross ?? '';\n    const gBA = it.BA?.gross ?? '';\n    const stG = (gBL && gAD && gBA && gBL===gAD && gBL===gBA) ? 'OK' : ((gAD && gBA && gAD===gBA) ? 'OK' : 'REVISAR');\n\n    equiposRows.push({ Dato: `${label} - Seal`, BL: sealBL||'SIN DATO', Aduana: sealAD||'SIN DATO', 'Booking Advice': sealBA||'SIN DATO', Estado: 'OK', Nota: sealNote });\n    equiposRows.push({ Dato: `${label} - Net (KG)`, BL: String(nBL||''), Aduana: String(nAD||''), 'Booking Advice': String(nBA||''), Estado: stN, Nota: '' });\n    equiposRows.push({ Dato: `${label} - Gross (KG)`, BL: String(gBL||''), Aduana: String(gAD||''), 'Booking Advice': String(gBA||''), Estado: stG, Nota: '' });\n  }\n\n  // Unir filas\n  const compare_excel_pairs = [...rows, ...equiposRows];\n\n  // --------- Resumen clave ----------\n  const famBL = bl?.desc?.['DESC BL - PRODUCTO'] || '';\n  const famAD = adu?.contenedores?.[0]?.producto || '';\n  const famBA = ba?.producto?.familia || '';\n\n  const compare_summary = {\n    key_fields: {\n      order_number: { BL: orderBL || '', BA: orderBA || '', Aduana: orderAD || '' },\n      booking_no:   { BL: bookingBL || '', BA: bookingBA || '' },\n      bl_number:    bl.bl_no || '',\n      incoterm:     { BL: blIncotermShown, BA: baIncoterm },\n      incoterm_place: { BL: blIncPlace, BA: baIncPlace },\n      shipper: buildBlock(bl.shipper || ''),\n      pol: { BL: bl.pol || '', BA: ba.pol || '' },\n      pod: { BL: bl.pod || '', BA: ba.pod || '' },\n      destino: { Aduana: aduPais || '', BA: baPais || '' },\n      consignee: { BL: buildBlock(bl.consignee || ''), BA: buildBlock(ba.consignee || {}) },\n      notify: { BL: buildBlock(bl.notify || ''), BA: buildBlock(ba.notify || {}) },\n      consignee_tax: { BL: cleanDigits((/CNPJ\\s*([\\d./-]+)/i.exec(String(bl.consignee || ''))||[])[1] || ''), BA: cleanDigits(ba?.consignee?.tax_id || '') },\n      notify_tax: { BL: cleanDigits((/(TAX\\s*ID|CNPJ)\\s*[: ]*\\s*([0-9./-]+)/i.exec(String(bl.notify || ''))||[])[2] || ''), BA: cleanDigits(ba?.notify?.tax_id || '') },\n      notify_email: { BL: (String(bl.notify || '').match(/[A-Z0-9._%+-]+@[A-Z0-9.-]+\\.[A-Z]{2,}/ig)||[])[0] || '', BA: ba?.notify?.email || '' },\n      totals: {\n        BL: { bultos: bultosBL || '', net: bl?.desc?.['DESC BL - PESO NETO TOTAL (KG)'] || '', gross: bl?.desc?.['DESC BL - PESO BRUTO TOTAL (KG)'] || '' },\n        Aduana: { bultos: adu?.totals?.bultos ?? '', net: adu?.totals?.neto ?? '', gross: adu?.totals?.bruto ?? '' },\n        BA: { bultos: ba?.totales?.piece_count ?? '', net: ba?.totales?.net_kg ?? '', gross: ba?.totales?.gross_kg ?? '' }\n      },\n      producto: {\n        descripcion_completa: { BL: prodBLFull, BA: prodBAFull, Aduana: aduProds },\n        BL: { familia: famBL, grado: bl?.desc?.['DESC BL - GRADE / CALIDAD'] || '', embalaje: bl?.desc?.['DESC BL - TIPO DE EMBALAJE'] || '' },\n        Aduana: { producto: famAD || '', grado: '' },\n        BA: { familia: famBA, grado: ba?.producto?.grado || '', embalaje: ba?.producto?.embalaje || '' },\n      },\n      hs_ncm_4d: { BL: bl?.desc?.['DESC BL - NCM'] || '', BA: ba?.hs?.import || ba?.ncm_export || '' },\n      pe: { BL: bl?.desc?.['DESC BL - PE (PERMISO DE EMBARQUE)'] || '', Aduana: adu?.ddt || '' },\n      bl_charges: {\n        thc_origen: toNum(blThcOr) || 0,\n        thc_destino: toNum(blThcDe) || 0,\n        cantidad_contenedores: ((bl?.freight?.containers_for_calc ?? contBL.length) || 0),\n        flete_por_contenedor: bl?.freight?.per_container?.USD ?? 0,\n        flete_total: blFleteTotal || 0\n      }\n    },\n    overall: counters.REVISAR > 0 ? 'REVISAR' : 'OK'\n  };\n\n  // Comparativa de equipos\n  const compare_equipos = buildCompareEquipos(bl, ba, adu);\n\n  // Contadores globales\n  const compare = {\n    overall: counters.REVISAR > 0 ? 'REVISAR' : 'OK',\n    counters: { OK: counters.OK, REVISAR: counters.REVISAR },\n    notes: counters.REVISAR > 0 ? 'Existen diferencias para revisar (ver filas Estado=REVISAR).' : 'Sin diferencias relevantes.'\n  };\n\n  return { compare_excel_pairs: [...rows, ...equiposRows], compare_summary, compare_equipos, compare };\n}\n\n/* =========================\n   n8n Code node (Run Once for Each Item)\n   ========================= */\nconst current = $input.item;\nconst doc = current.json || current;\nconst result = buildComparison(doc);\nreturn { json: { ...doc, ...result } };\n"}, position: [3584, -512] },
  output: [{}]
});

const parserBookingCode_22 = node({
  type: "n8n-nodes-base.code",
  version: 2,
  config: { name: "Parser Booking (Code)", parameters: {"mode": "runOnceForEachItem", "jsCode": "// n8n Code (Node.js) — Run Once for Each Item\n// Espera en item.json: { text, id, name, mimeType, webViewLink, numpages, numrender, info, metadata, version }\n// Devuelve el mismo objeto + booking_extract, booking_excel_pairs, cmp_ready\n\n// ---------- Helpers ----------\nfunction norm(s = \"\") {\n  return String(s).replace(/\\r/g, \"\").trim();\n}\nfunction linesOf(s = \"\") {\n  return norm(s).split(\"\\n\");\n}\nfunction firstNonEmpty(arr, startIdx = 0) {\n  for (let i = startIdx; i < arr.length; i++) {\n    const t = (arr[i] || \"\").trim();\n    if (t) return { value: t, index: i };\n  }\n  return { value: \"\", index: -1 };\n}\nfunction getNextLineAfter(text, labelRe) {\n  const idx = text.search(labelRe);\n  if (idx < 0) return \"\";\n  const tail = text.slice(idx).split(\"\\n\");\n  const { value } = firstNonEmpty(tail, 1);\n  return value || \"\";\n}\n// Números con separador de miles \",\" y decimales \".\": 22,140.000 -> 22140.000\nfunction parseNum(s) {\n  if (!s) return null;\n  const cleaned = String(s).replace(/,/g, \"\").match(/[\\d.]+/g);\n  if (!cleaned) return null;\n  const str = cleaned.join(\"\");\n  const n = parseFloat(str);\n  return Number.isFinite(n) ? n : null;\n}\nfunction parseKg(s)   { const n = parseNum(s); return n != null ? n : 0; }\nfunction parseCd3(s)  { const n = parseNum(s); return n != null ? n : 0; }\n\nfunction looksLikeAddress(t) {\n  if (!t) return false;\n  const s = t.trim();\n  if (/\\d/.test(s)) return true; // números (nro, CP, km, etc.)\n  if (/^(ROD|RUA|AV\\.?|KM|CALLE|ALAMEDA|ESTR\\.|CAMINO|PASAJE)\\b/i.test(s)) return true;\n  if (/-\\s*[A-Z]{2}\\b/.test(s)) return true;       // ... - SC\n  if (/\\b\\d{5}-\\d{3}\\b/.test(s)) return true;      // CEP\n  return false;\n}\n\n// ---------- Extractores de bloques ----------\nfunction extractConsigneeBlock(text) {\n  const out = { name: \"\", tax_id: \"\", address_lines: [], address_str: \"\" };\n  const reStart = /Ship-to\\s*\\/\\s*Consignee[^\\n]*\\n?/i;\n  const idx = text.search(reStart);\n  if (idx < 0) return out;\n\n  const tail = text.slice(idx).split(\"\\n\");\n\n  // Tax ID (misma línea o siguiente)\n  const headLine = tail[0] || \"\";\n  let i = 1;\n  let mTax = headLine.match(/(\\d{10,})/);\n  if (!mTax && tail[i]) {\n    const m = (tail[i] || \"\").match(/(\\d{10,})/);\n    if (m) { mTax = m; i++; }\n  }\n  if (mTax) out.tax_id = mTax[1];\n\n  // Armar nombre (puede venir en varias líneas)\n  const nameLines = [];\n  for (; i < tail.length; i++) {\n    const t = (tail[i] || \"\").trim();\n    if (!t) continue;\n\n    // Cortes de bloque\n    if (/^(Terms\\s*of\\s*Delivery|Carrier|Sold-to|Customer\\s*Tax\\s*ID|Port\\s*of\\s*Discharge|Port\\s*of\\s*Loading)/i.test(t)) break;\n\n    // Si parece dirección, frenamos el nombre\n    if (looksLikeAddress(t)) break;\n\n    // Si es email/phone/taxid, no es nombre\n    if (/E-?Mail|Ph\\s*No\\.?|Tax\\s*ID/i.test(t)) break;\n\n    // Si no hay dígitos, casi seguro es parte del nombre\n    if (!/\\d/.test(t)) {\n      nameLines.push(t);\n      continue;\n    }\n\n    // Cualquier otra cosa probablemente sea dirección\n    break;\n  }\n\n  if (nameLines.length === 0 && i < tail.length && (tail[i] || \"\").trim()) {\n    const t = (tail[i] || \"\").trim();\n    if (!looksLikeAddress(t)) { nameLines.push(t); i++; }\n  }\n\n  out.name = nameLines.join(\" \").replace(/\\s+/g, \" \").trim();\n\n  // Dirección hasta el siguiente corte\n  for (; i < tail.length; i++) {\n    const t = (tail[i] || \"\").trim();\n    if (!t) continue;\n    if (/^(Terms\\s*of\\s*Delivery|Carrier|Sold-to|Customer\\s*Tax\\s*ID|Port\\s*of\\s*Discharge|Port\\s*of\\s*Loading)/i.test(t)) break;\n    out.address_lines.push(t);\n  }\n  out.address_str = out.address_lines.filter(Boolean).join(\", \");\n  return out;\n}\n\nfunction extractNotifyBlock(text) {\n  const out = { name: \"\", address_lines: [], address_str: \"\", email: \"\", tax_id: \"\", phone: \"\" };\n  const idx = text.search(/Notify\\s+Party/i);\n  if (idx < 0) return out;\n  const tail = text.slice(idx).split(\"\\n\");\n\n  let i = 1;\n  // Nombre\n  const fn = firstNonEmpty(tail, i);\n  out.name = fn.value || \"\";\n  i = (fn.index >= 0 ? fn.index + 1 : i);\n\n  // Dirección + campos\n  for (; i < tail.length; i++) {\n    const raw = tail[i] || \"\";\n    const t = raw.trim();\n    if (!t) continue;\n\n    // Cortes de bloque/sección siguientes\n    if (/^(SET\\s+OF\\s+DOCUMENTS|SPECIAL\\s+INSTRUCTIONS|Shipping\\s+Dtl\\s+Recip1|Document\\s+Recip|3\\)|Consolidated Booking Advice|Terms of Delivery|Carrier|Sold-to|Port of)/i.test(t)) break;\n\n    // Campos conocidos\n    const mEmail = t.match(/E-?Mail\\s*:\\s*([^\\s;]+@[^\\s;]+)/i);\n    if (mEmail) { out.email = mEmail[1]; continue; }\n\n    const mPh = t.match(/Ph\\s*No\\.?\\s*:\\s*(.+)$/i);\n    if (mPh) { out.phone = mPh[1].trim(); continue; }\n\n    const mTax = t.match(/Tax\\s*ID\\s*:\\s*([0-9\\-\\.]+)/i);\n    if (mTax) { out.tax_id = mTax[1].replace(/[^\\d]/g, \"\"); continue; }\n\n    // Acumular dirección\n    out.address_lines.push(t);\n  }\n  out.address_str = out.address_lines.filter(Boolean).join(\", \");\n  return out;\n}\n\nfunction extractDocRecipients(text) {\n  const sections = [\"Shipping Dtl Recip1\", \"Document Recip Orig\", \"Document Recip Copy2\"];\n  const recipients = [];\n\n  for (const sec of sections) {\n    const re = new RegExp(sec.replace(/[.*+?^${}()|[\\]\\\\]/g, \"\\\\$&\") + \"\\\\s*\\\\n([\\\\s\\\\S]{0,200})\", \"i\");\n    const m = text.match(re);\n    if (!m) continue;\n\n    const block = (m[1] || \"\").split(\"\\n\");\n    const nm = firstNonEmpty(block, 0).value;\n    let email = \"\";\n    for (const ln of block) {\n      const mm = (ln || \"\").match(/E-?Mail\\s*:\\s*([^\\s;]+@[^\\s;]+)/i);\n      if (mm) { email = mm[1]; break; }\n    }\n    if (nm && !/CONTACT/i.test(sec)) {\n      recipients.push({ section: sec, name: nm.trim(), email });\n    }\n  }\n  return recipients;\n}\n\nfunction extractProducto(text) {\n  // Tomamos la primera línea que contenga \"Polyethylene\" y \"Big Bag\"\n  const mCad = text.match(/Polyethylene[^\\n]*Big\\s*Bag[^\\n]*/i);\n  const cadena = mCad ? mCad[0].trim() : \"\";\n\n  // Grado\n  let grado = \"\";\n  const mGr = cadena.match(/Polyethylene\\s+([A-Z0-9]+)\\b/i);\n  if (mGr) grado = mGr[1];\n\n  // Familia\n  const familia = \"POLYETHYLENE\";\n\n  // Embalaje\n  let embalaje = \"\";\n  if (/Big\\s*Bag/i.test(cadena)) embalaje = \"Big Bag\";\n\n  return { cadena, familia, grado, embalaje };\n}\n\nfunction extractTotals(text) {\n  const mGW = text.match(/Total\\s+Gross\\s+weight:\\s*([\\d,\\.]+)\\s*KG/i);\n  const mNW = text.match(/Total\\s+Net\\s+weight:\\s*([\\d,\\.]+)\\s*KG/i);\n  const mVOL = text.match(/Total\\s+Volume:\\s*([\\d,\\.]+)\\s*CD3/i);\n  const mPC = text.match(/Piece\\s+Count\\s*:\\s*([\\d,\\.]+)/i);\n\n  return {\n    piece_count: mPC ? parseNum(mPC[1]) : 0,\n    net_kg: mNW ? parseKg(mNW[1]) : 0,\n    gross_kg: mGW ? parseKg(mGW[1]) : 0,\n    volume_cd3: mVOL ? parseCd3(mVOL[1]) : 0,\n  };\n}\n\nfunction extractEquipos(text) {\n  const equipos = [];\n  const contRe = /^([A-Z]{4}\\d{7})\\b.*$/gm;\n  const matches = [];\n  let m;\n  while ((m = contRe.exec(text)) !== null) {\n    matches.push({ container: m[1], index: m.index });\n  }\n  // Añadir final para el último corte\n  matches.push({ container: null, index: text.length });\n\n  for (let i = 0; i < matches.length - 1; i++) {\n    const c = matches[i].container;\n    const start = matches[i].index;\n    const end = matches[i + 1].index;\n    const seg = text.slice(start, end);\n\n    // Seal\n    const mSeal = seg.match(/Seal\\s+Number\\s*:\\s*([A-Z0-9-]+)/i);\n    const seal = mSeal ? mSeal[1] : \"\";\n\n    // Type\n    const mType = seg.match(/Equipment\\s+Type\\s*:\\s*([^\\n]+)/i);\n    const type = mType ? mType[1].trim() : \"\";\n\n    // Bultos\n    const mBultos = seg.match(/(\\d+)\\s*BAG/i);\n    const bultos = mBultos ? parseInt(mBultos[1], 10) : null;\n\n    // Pesos/Volumen\n    const mINW = seg.match(/Item\\s+Net\\s+Weight\\s*:\\s*([\\d,\\.]+)\\s*KG/i);\n    const mIGW = seg.match(/Item\\s+Gross\\s+Weight\\s*:\\s*([\\d,\\.]+)\\s*KG/i);\n    const mIV  = seg.match(/Item\\s+Volume\\s*:\\s*([\\d,\\.]+)\\s*CD3/i);\n\n    let net_kg = mINW ? parseKg(mINW[1]) : null;\n    let gross_kg = mIGW ? parseKg(mIGW[1]) : null;\n    let volume_cd3 = mIV ? parseCd3(mIV[1]) : null;\n\n    // Fallbacks\n    if (net_kg == null) {\n      const mNW2 = seg.match(/Net\\s+Weight\\s+([\\d,\\.]+)\\s*KG/i);\n      if (mNW2) net_kg = parseKg(mNW2[1]);\n    }\n    if (gross_kg == null) {\n      const mGW2 = seg.match(/Gross\\s+Weight\\s+([\\d,\\.]+)\\s*KG/i);\n      if (mGW2) gross_kg = parseKg(mGW2[1]);\n    }\n    if (volume_cd3 == null) {\n      const mV2 = seg.match(/Volume\\s+([\\d,\\.]+)\\s*CD3/i);\n      if (mV2) volume_cd3 = parseCd3(mV2[1]);\n    }\n\n    // Si falta algo, lo dejamos en 0 para no romper sumas\n    equipos.push({\n      container: c,\n      seal,\n      type: type || \"40' Container\",\n      bultos: bultos != null ? bultos : 0,\n      net_kg: net_kg != null ? net_kg : 0,\n      gross_kg: gross_kg != null ? gross_kg : 0,\n      volume_cd3: volume_cd3 != null ? volume_cd3 : 0,\n    });\n  }\n\n  return equipos;\n}\n\nfunction sumEquipos(equipos) {\n  return equipos.reduce((acc, e) => {\n    acc.bultos += e.bultos || 0;\n    acc.net_kg += e.net_kg || 0;\n    acc.gross_kg += e.gross_kg || 0;\n    acc.volume_cd3 += e.volume_cd3 || 0;\n    return acc;\n  }, { bultos: 0, net_kg: 0, gross_kg: 0, volume_cd3: 0 });\n}\n\n// ====== NUEVO: extracción robusta del ORDER NUMBER ======\nfunction digitsOnly(s){ return String(s||\"\").replace(/[^\\d]/g, \"\"); }\n\nfunction orderFromName(name){\n  // primer bloque de 8–12 dígitos en el nombre del archivo\n  const m = String(name||\"\").match(/(?<!\\d)(\\d{8,12})(?!\\d)/);\n  return m ? m[1] : null;\n}\n\nfunction orderFromText(text){\n  const t = norm(text);\n  // variantes frecuentes\n  const m1 = t.match(/\\bOrder\\s*Number\\s*:\\s*([A-Z0-9\\-]{6,})/i);\n  if (m1) return digitsOnly(m1[1]);\n  const m2 = t.match(/\\bOrder\\s*No\\.?\\s*:\\s*([A-Z0-9\\-]{6,})/i);\n  if (m2) return digitsOnly(m2[1]);\n  const m3 = t.match(/\\bCustomer\\s*(?:Order|Reference)\\s*:\\s*([A-Z0-9\\-]{6,})/i);\n  if (m3) return digitsOnly(m3[1]);\n  return null;\n}\n// ========================================================\n\n// ---------- Principal ----------\nconst input = item.json ?? item; // n8n entrega normalmente en item.json\nconst text = norm(input.text || \"\");\n\n// ORDER NUMBER: texto → nombre de archivo → vacío\nlet order_number = orderFromText(text) || orderFromName(input.name) || \"\";\norder_number = digitsOnly(order_number); // normalizar a dígitos para el joinKey\n\nconst shipment_number = getNextLineAfter(text, /Shipment\\s+Number/i);\nconst booking_no = (text.match(/BOOKING\\s+NUMBER\\s*:\\s*([A-Z0-9\\-]+)/i) || [])[1] || \"\";\n\nconst transport_mode = getNextLineAfter(text, /Transport\\s+Mode/i);\nconst containers_qty = parseInt(((text.match(/CONTAINERS\\s+ON\\s+BOARD\\s*:\\s*0*([0-9]+)/i) || [])[1] || \"0\"), 10);\n\nconst pol = getNextLineAfter(text, /Port\\s+of\\s+Loading/i);\nconst pod = getNextLineAfter(text, /Port\\s+of\\s+Discharge/i);\n\n// Fechas\nconst document_date = getNextLineAfter(text, /Document\\s+Date/i);\nconst shipment_date = getNextLineAfter(text, /Shipment\\s+Date/i);\nconst delivery_to_dest_port = getNextLineAfter(text, /Delivery\\s+Date\\s+to\\s+Destination\\s+Port/i);\nconst final_dest_delivery_date = getNextLineAfter(text, /Final\\s+Destination\\s+Delivery\\s+Date/i);\nconst cutoff_origin = (text.match(/CUTOFF\\s+AT\\s+ORIGIN\\s*:\\s*(\\d{8})/i) || [])[1] || \"\";\nconst etd_pol = (text.match(/ETD\\s+PORT\\s+OF\\s+LOAD\\s*:\\s*(\\d{8})/i) || [])[1] || \"\";\nconst eta_destination = (text.match(/ETA\\s+DESTINATION\\s*:\\s*(\\d{8})/i) || [])[1] || \"\";\n\n// Consignee & Notify\nconst consignee = extractConsigneeBlock(text);\nconst notify = extractNotifyBlock(text);\n\n// País destino (última línea de la dirección del consignee suele ser el país)\nlet destino_pais = \"\";\nif (consignee.address_lines.length) {\n  const last = consignee.address_lines[consignee.address_lines.length - 1].toUpperCase();\n  if (/BRAZIL|BRASIL/.test(last)) destino_pais = \"BRAZIL\";\n  else destino_pais = last.replace(/[^A-Z]/g, \"\") || \"BRAZIL\";\n}\n\n// Producto\nconst producto = extractProducto(text);\n\n// Equipos y totales\nlet equipos = extractEquipos(text);\nconst totalsFromDoc = extractTotals(text);\nconst totalsFromEquipos = sumEquipos(equipos);\n\n// Si por alguna razón no se detectó bien en doc, usamos sumatoria de equipos\nconst totales = {\n  piece_count: totalsFromDoc.piece_count || totalsFromEquipos.bultos,\n  net_kg: totalsFromDoc.net_kg || totalsFromEquipos.net_kg,\n  gross_kg: totalsFromDoc.gross_kg || totalsFromEquipos.gross_kg,\n  volume_cd3: totalsFromDoc.volume_cd3 || totalsFromEquipos.volume_cd3,\n};\n\n// HS / NCM\nconst hs_export = (text.match(/Export:\\s*([0-9A-Z]+)/i) || [])[1] || \"\";\nconst hs_import = (text.match(/Import:\\s*([0-9A-Z]+)/i) || [])[1] || \"\";\n\n// Document recipients (sin CONTACT)\nconst doc_recipients = extractDocRecipients(text);\n\n// Armado de objetos de salida\nconst booking_extract = {\n  order_number,\n  shipment_number,\n  booking_no,\n  transport_mode,\n  containers_qty: Number.isFinite(containers_qty) ? containers_qty : (equipos?.length || 0),\n  pol,\n  pod,\n  destino_pais,\n  vessel: \"\", // no presente en el PDF\n  voyage: \"\", // no presente en el PDF\n  dates: {\n    document_date,\n    shipment_date,\n    cutoff_origin,\n    etd_pol,\n    delivery_to_dest_port,\n    eta_destination,\n    final_dest_delivery_date,\n  },\n  consignee,\n  notify,\n  producto,\n  totales,\n  hs: { export: hs_export, import: hs_import },\n  ncm_export: hs_export,\n  equipos,\n  terms_of_delivery: getNextLineAfter(text, /Terms\\s+of\\s+Delivery/i),\n  incoterm: ((getNextLineAfter(text, /Terms\\s+of\\s+Delivery/i) || \"\").split(/\\s+/)[0] || \"\").toUpperCase(),\n  incoterm_place: (getNextLineAfter(text, /Terms\\s+of\\s+Delivery/i) || \"\").replace(/^[A-Z]+\\s*/i, \"\").trim(),\n  document_recipients: doc_recipients,\n  links: {\n    fileId: input.id || \"\",\n    name: input.name || \"\",\n    webViewLink: input.webViewLink || \"\",\n  },\n};\n\nconst booking_excel_pairs = [\n  { Dato: \"BA - ORDER NUMBER\", Valor: booking_extract.order_number },\n  { Dato: \"BA - SHIPMENT NUMBER\", Valor: booking_extract.shipment_number },\n  { Dato: \"BA - BOOKING NUMBER\", Valor: booking_extract.booking_no },\n  { Dato: \"BA - TRANSPORT MODE\", Valor: booking_extract.transport_mode },\n  { Dato: \"BA - CONTAINERS (QTY)\", Valor: String(booking_extract.containers_qty || (equipos?.length || 0)) },\n  { Dato: \"BA - POL\", Valor: booking_extract.pol },\n  { Dato: \"BA - POD\", Valor: booking_extract.pod },\n  { Dato: \"BA - DESTINO (PAÍS)\", Valor: booking_extract.destino_pais },\n  { Dato: \"BA - VESSEL\", Valor: booking_extract.vessel },\n  { Dato: \"BA - VOYAGE\", Valor: booking_extract.voyage },\n  { Dato: \"BA - DOCUMENT DATE\", Valor: booking_extract.dates.document_date },\n  { Dato: \"BA - SHIPMENT DATE\", Valor: booking_extract.dates.shipment_date },\n  { Dato: \"BA - CUTOFF ORIGIN\", Valor: booking_extract.dates.cutoff_origin },\n  { Dato: \"BA - ETD POL\", Valor: booking_extract.dates.etd_pol },\n  { Dato: \"BA - DELIVERY DATE TO DESTINATION PORT\", Valor: booking_extract.dates.delivery_to_dest_port },\n  { Dato: \"BA - ETA DESTINATION\", Valor: booking_extract.dates.eta_destination },\n  { Dato: \"BA - FINAL DESTINATION DELIVERY DATE\", Valor: booking_extract.dates.final_dest_delivery_date },\n  { Dato: \"BA - CONSIGNEE\", Valor: booking_extract.consignee.name },\n  { Dato: \"BA - CONSIGNEE TAX ID (CNPJ)\", Valor: booking_extract.consignee.tax_id || \"\" },\n  { Dato: \"BA - CONSIGNEE (FULL)\", Valor: [booking_extract.consignee.name, ...booking_extract.consignee.address_lines].filter(Boolean).join(\"\\n\") },\n  { Dato: \"BA - NOTIFY PARTY\", Valor: [booking_extract.notify.name, ...booking_extract.notify.address_lines].filter(Boolean).join(\"\\n\") },\n  { Dato: \"BA - NOTIFY TAX ID\", Valor: booking_extract.notify.tax_id || \"\" },\n  { Dato: \"BA - NOTIFY EMAIL\", Valor: booking_extract.notify.email || \"\" },\n  { Dato: \"BA - TERMS OF DELIVERY (INCOTERM)\", Valor: booking_extract.terms_of_delivery },\n  { Dato: \"(Opcional) BA - INCOTERM\", Valor: booking_extract.incoterm },\n  { Dato: \"(Opcional) BA - INCOTERM PLACE\", Valor: booking_extract.incoterm_place },\n  { Dato: \"BA - PRODUCTO (CADENA)\", Valor: booking_extract.producto.cadena },\n  { Dato: \"BA - FAMILIA (NORMALIZADA)\", Valor: booking_extract.producto.familia },\n  { Dato: \"BA - GRADO\", Valor: booking_extract.producto.grado },\n  { Dato: \"BA - EMBALAJE\", Valor: booking_extract.producto.embalaje },\n  { Dato: \"BA - PIECE COUNT (BULTOS)\", Valor: String(booking_extract.totales.piece_count) },\n  { Dato: \"BA - TOTAL NET WEIGHT (KG)\", Valor: String(booking_extract.totales.net_kg) },\n  { Dato: \"BA - TOTAL GROSS WEIGHT (KG)\", Valor: String(booking_extract.totales.gross_kg) },\n  { Dato: \"BA - TOTAL VOLUME (CD3)\", Valor: String(booking_extract.totales.volume_cd3) },\n  { Dato: \"BA - NCM EXPORT\", Valor: booking_extract.ncm_export },\n  { Dato: \"BA - HS IMPORT\", Valor: booking_extract.hs.import },\n  { Dato: \"BA - LINK (DRIVE)\", Valor: booking_extract.links.webViewLink },\n];\n\n// Equipos en Excel Pairs\nequipos.forEach((eq, idx) => {\n  const n = idx + 1;\n  booking_excel_pairs.push({ Dato: `BA - Equipo ${n} - Container`, Valor: eq.container || \"\" });\n  booking_excel_pairs.push({ Dato: `BA - Equipo ${n} - Seal`, Valor: eq.seal || \"\" });\n  booking_excel_pairs.push({ Dato: `BA - Equipo ${n} - Type`, Valor: eq.type || \"\" });\n  booking_excel_pairs.push({ Dato: `BA - Equipo ${n} - Bultos`, Valor: String(eq.bultos || 0) });\n  booking_excel_pairs.push({ Dato: `BA - Equipo ${n} - Net (KG)`, Valor: String(eq.net_kg || 0) });\n  booking_excel_pairs.push({ Dato: `BA - Equipo ${n} - Gross (KG)`, Valor: String(eq.gross_kg || 0) });\n  booking_excel_pairs.push({ Dato: `BA - Equipo ${n} - Volume (CD3)`, Valor: String(eq.volume_cd3 || 0) });\n});\n\n// Doc recipients en Excel Pairs (sin CONTACT)\ndoc_recipients.forEach((r, idx) => {\n  const n = idx + 1;\n  booking_excel_pairs.push({ Dato: `BA - DOC RECIP ${n} - Section`, Valor: r.section || \"\" });\n  booking_excel_pairs.push({ Dato: `BA - DOC RECIP ${n} - Name`, Valor: r.name || \"\" });\n  booking_excel_pairs.push({ Dato: `BA - DOC RECIP ${n} - Email`, Valor: r.email || \"\" });\n});\n\nconst cmp_ready = {\n  vessel: \"\",\n  voyage: \"\",\n  pol: (booking_extract.pol || \"\").toUpperCase(),\n  pod: (booking_extract.pod || \"\").toUpperCase(),\n  place_delivery: null,\n  containers_count: booking_extract.containers_qty || (equipos?.length || 0),\n  product_grade: booking_extract.producto.grado || \"\",\n  product_family: booking_extract.producto.familia || \"\",\n  total_bultos: booking_extract.totales.piece_count,\n  total_nw: booking_extract.totales.net_kg,\n  total_gw: booking_extract.totales.gross_kg,\n  container_type_hint: /40/.test(equipos.map(e => e.type).join(\" \")) ? \"40\" : \"\",\n  notify: {\n    name: booking_extract.notify.name || \"\",\n    tax_id: booking_extract.notify.tax_id || \"\",\n    email: booking_extract.notify.email || \"\",\n    address_lines: booking_extract.notify.address_lines || [],\n    address_str: booking_extract.notify.address_str || \"\",\n  },\n  consignee: {\n    name: booking_extract.consignee.name || \"\",\n    tax_id: booking_extract.consignee.tax_id || \"\",\n    address_lines: booking_extract.consignee.address_lines || [],\n    address_str: booking_extract.consignee.address_str || \"\",\n  },\n  equipos: equipos.map(e => ({\n    container: e.container,\n    seal: e.seal,\n    type: e.type,\n    bultos: e.bultos,\n    nw: e.net_kg,\n    gw: e.gross_kg,\n    volume_cd3: e.volume_cd3,\n  })),\n  doc_recipients: doc_recipients.map(r => ({ section: r.section, name: r.name, email: r.email })),\n  ncm_export: booking_extract.ncm_export || \"\",\n};\n\n// Construir output final fusionando el input original\nconst output = {\n  ...input,\n  booking_extract,\n  booking_excel_pairs,\n  cmp_ready,\n};\n\n// IMPORTANTE (Run Once for Each Item): devolver **un objeto** con { json: ... }\nreturn { json: output };\n"}, position: [2752, -288] },
  output: [{}]
});

const claudeModel = languageModel({
  type: '@n8n/n8n-nodes-langchain.lmChatAnthropic',
  version: 1.5,
  config: {
    name: 'Claude Sonnet 4.6',
    parameters: { model: { __rl: true, mode: 'list', value: 'claude-sonnet-4-6', cachedResultName: 'Claude Sonnet 4.6' }, options: { maxTokensToSample: 4096, temperature: 0, thinkingMode: 'disabled' } },
    position: [2600, -300],
    credentials: { anthropicApi: newCredential('Anthropic Claude API') }
  },
  output: [{}]
});

const aduanaSchema = outputParser({
  type: '@n8n/n8n-nodes-langchain.outputParserStructured',
  version: 1.3,
  config: {
    name: 'Aduana Schema',
    parameters: { schemaType: 'manual', inputSchema: "{\n  \"type\": \"object\",\n  \"properties\": {\n    \"aduana_extract\": {\n      \"type\": \"object\",\n      \"properties\": {\n        \"operacion\": { \"type\": [\"string\", \"null\"] },\n        \"buque\": { \"type\": [\"string\", \"null\"] },\n        \"destino\": { \"type\": [\"string\", \"null\"] },\n        \"ddt\": { \"type\": [\"string\", \"null\"] },\n        \"totals\": {\n          \"type\": \"object\",\n          \"properties\": {\n            \"bultos\": { \"type\": [\"number\", \"null\"] },\n            \"neto\": { \"type\": [\"number\", \"null\"] },\n            \"bruto\": { \"type\": [\"number\", \"null\"] }\n          },\n          \"required\": [\"bultos\", \"neto\", \"bruto\"]\n        },\n        \"contenedores\": {\n          \"type\": \"array\",\n          \"items\": {\n            \"type\": \"object\",\n            \"properties\": {\n              \"container\": { \"type\": [\"string\", \"null\"] },\n              \"precinto\": { \"type\": [\"string\", \"null\"] },\n              \"producto\": { \"type\": [\"string\", \"null\"] },\n              \"neto\": { \"type\": [\"number\", \"null\"] },\n              \"bruto\": { \"type\": [\"number\", \"null\"] }\n            },\n            \"required\": [\"container\", \"precinto\", \"producto\", \"neto\", \"bruto\"]\n          }\n        }\n      },\n      \"required\": [\"operacion\", \"buque\", \"destino\", \"ddt\", \"totals\", \"contenedores\"]\n    }\n  },\n  \"required\": [\"aduana_extract\"]\n}\n", autoFix: false },
    position: [2760, -300]
  },
  output: [{}]
});

const parserAduanaIA = node({
  type: '@n8n/n8n-nodes-langchain.chainLlm',
  version: 1.9,
  config: {
    name: 'Parser Aduana (IA)',
    parameters: {
      promptType: 'define',
      text: '={{ $json.text }}',
      hasOutputParser: true,
      messages: { messageValues: [{ type: 'SystemMessagePromptTemplate', message: "Sos un extractor de datos de planillas de aduana de exportación argentinas (texto plano extraído de un PDF). Tu única salida es un objeto JSON que cumpla EXACTAMENTE el schema de abajo. Seguí estas 10 reglas duras:\n\n1. SALIDA: devolvé SOLO el objeto JSON. Sin prosa, sin explicación, sin ``` ni markdown.\n2. RAÍZ: el objeto raíz tiene una sola clave, \"aduana_extract\".\n3. TRANSCRIPCIÓN LITERAL: copiá los valores tal como aparecen en el texto. NUNCA corrijas    errores de tipeo aparentes. Ej.: si el buque dice \"LOG IK\", devolvé \"LOG IK\" — NO lo    \"arregles\" a \"LOG IN\".\n4. MAYÚSCULAS: buque, destino, ddt, producto y precinto van en MAYÚSCULAS.\n5. NORMALIZACIÓN DE PAÍS: el único cambio permitido además de mayúsculas es normalizar el    país de DESTINO a inglés canónico: BRASIL→BRAZIL, ESPAÑA→SPAIN, ESTADOS UNIDOS→USA,    REINO UNIDO→UK, ALEMANIA→GERMANY, FRANCIA→FRANCE, ITALIA→ITALY. Si ya está en inglés o    no está en la lista, dejalo en MAYÚSCULAS sin tocar.\n6. OPERACIÓN: tomá el número de ORDEN/OPERACIÓN (7 a 12 dígitos). En \"operacion\" devolvé    SOLO los dígitos (sin guiones, espacios ni letras).\n7. CONTENEDORES: array. Cada elemento: container (patrón ^[A-Z]{4}\\d{7}$, ej. TIIU5116765),    precinto (alfanumérico en mayúsculas), producto (descripción en mayúsculas), neto (peso    neto en kg, número), bruto (peso bruto en kg, número).\n8. TOTALES: \"totals\" = suma sobre TODOS los contenedores. bultos = suma de la cantidad de    bultos de cada contenedor; neto = suma de pesos netos; bruto = suma de pesos brutos.    Calculá la suma — no copies una sola fila a ciegas.\n9. NÚMEROS: devolvé números JSON (no strings). El texto puede usar formato europeo (punto    como separador de miles, coma como decimal): \"27.540,5\" → 27540.5. \"27000\" → 27000.\n10. FALTANTES: si un campo no está en el texto, devolvé null. Las filas de contenedor     pueden venir en varias líneas (tipo, container+precinto, producto y números en líneas     separadas) o todo en una línea: manejá ambos casos.\n\nSCHEMA EXACTO (forma y tipos):\n{\n  \"aduana_extract\": {\n    \"operacion\": \"string | null\",\n    \"buque\": \"string | null\",\n    \"destino\": \"string | null\",\n    \"ddt\": \"string | null\",\n    \"totals\": { \"bultos\": \"number | null\", \"neto\": \"number | null\", \"bruto\": \"number | null\" },\n    \"contenedores\": [\n      { \"container\": \"string | null\", \"precinto\": \"string | null\",\n        \"producto\": \"string | null\", \"neto\": \"number | null\", \"bruto\": \"number | null\" }\n    ]\n  }\n}" }] }
    },
    position: [2752, -480],
    onError: 'continueRegularOutput',
    subnodes: { model: claudeModel, outputParser: aduanaSchema }
  },
  output: [{ output: { aduana_extract: {} } }]
});

const inyectarCode = node({
  type: 'n8n-nodes-base.code',
  version: 2,
  config: {
    name: 'Inyectar pe + source_link',
    parameters: { mode: 'runOnceForEachItem', language: 'javaScript', jsCode: "/**\n * NODO Code post-IA — \"Inyectar pe + source_link\"\n * Modo: Run Once for Each Item  ·  Lenguaje: JavaScript\n * onError: continueRegularOutput  (D2 = continue-on-fail con log explícito)\n *\n * Va ENTRE el \"Parser Aduana (IA)\" (Basic LLM Chain) y \"Set Aduana: Join Key\".\n *\n * Por qué existe (verificado contra el regex actual y el HTML del mail):\n *  - El chainLlm v1.9 NO preserva el input → se pierden los campos de Drive y los\n *    de la normalización (order_number, orden_from_name, webViewLink, fileId, ...).\n *    El regex hacía `return { json: { ...j, aduana_extract, ... } }` (spread del input).\n *    Acá restauramos ese passthrough leyendo del upstream real.\n *  - outputParserStructured envuelve la salida en una clave \"output\".\n *  - El HTML del mail (nodo intocable) lee adu.pe (L173) y adu.source_link (L136),\n *    que el LLM NO puede producir desde el texto:\n *      · pe         = alias de ddt           (regex L308: pe: ddt || null)\n *      · source_link = link de Drive del input (regex L298/L313 via pickDriveLink)\n */\n\n// pickDriveLink — copia EXACTA de la lógica del regex (Parser Aduana (Code) L228-238)\nfunction pickDriveLink(j) {\n  const cands = [];\n  if (j.webViewLink) cands.push(j.webViewLink);\n  if (j.links && j.links.webViewLink) cands.push(j.links.webViewLink);\n  if (j.aduana_webViewLink) cands.push(j.aduana_webViewLink);\n  if (j.aduana_links && j.aduana_links.webViewLink) cands.push(j.aduana_links.webViewLink);\n  if (cands.length > 0) return cands[0];\n\n  const fileId = j.fileId || (j.links && j.links.fileId) || (j.aduana_links && j.aduana_links.fileId);\n  if (fileId) return `https://drive.google.com/file/d/${fileId}/view?usp=drivesdk`;\n  return \"\";\n}\n\n// Input que el chainLlm descartó = mismo origen que leía el regex (j = su input).\n// El nodo inmediatamente anterior al parser era \"PDF — Extract From PDF (Aduana)\".\nlet up = {};\ntry {\n  up = $('PDF — Extract From PDF (Aduana)').item.json || {};\n} catch (e) {\n  // Si se rompe el pairing de items a través del chain, degradamos sin romper.\n  console.log('[Inyectar pe + source_link] no se pudo leer upstream PDF Aduana:', e.message);\n  up = {};\n}\n\n// Salida del LLM chain. outputParserStructured envuelve en \"output\";\n// el schema raíz es { aduana_extract: {...} }. Desenvolvemos defensivamente ambos niveles.\nconst root = ($json && $json.output) ? $json.output : $json;\nlet adu = (root && root.aduana_extract) ? root.aduana_extract : root;\n\n// D2 — continue-on-fail: si el parser IA no produjo un objeto válido, log + passthrough\n// sin romper. Set Aduana: Join Key usa optional chaining, soporta aduana_extract = null.\nif (!adu || typeof adu !== 'object' || Array.isArray(adu)) {\n  console.log('[Inyectar pe + source_link] aduana_extract ausente/inválido — continue-on-fail. $json:',\n    JSON.stringify($json).slice(0, 500));\n  return { json: { ...up, aduana_extract: null } };\n}\n\n// Inyectar lo que el LLM no puede sacar del texto:\nadu.pe = adu.ddt || null;                        // alias explícito (regex L308)\nadu.source_link = pickDriveLink(up) || null;     // link de Drive (regex L298/L313)\n\n// Restaurar el passthrough del input (paridad con `...j` del regex) + aduana_extract.\nreturn { json: { ...up, aduana_extract: adu } };\n" },
    position: [2900, -480],
    onError: 'continueRegularOutput'
  },
  output: [{}]
});

export default workflow("WVt6gvghL2nFVbt6", "control de bill of lading")
  .add(watchForNewFiles_0).to(codeNormalizadorNDeOrdenDesdeElNombre_1)
  .add(codeNormalizadorNDeOrdenDesdeElNombre_1).to(googleDriveDownload_2)
  .add(googleDriveDownload_2).to(extraerTextoDelPdf_3)
  .add(extraerTextoDelPdf_3).to(detector_4)
  .add(detector_4).to(switchRuteoPorNavieraValidacinDeOrden_5)
  .add(switchRuteoPorNavieraValidacinDeOrden_5.output(1).to(extractLogInCode_6))
  .add(extractLogInCode_6).to(googleDriveBuscarPlanillaDeAduana_7)
  .add(extractLogInCode_6).to(buscarBookingAdviceEnDrive_12)
  .add(extractLogInCode_6).to(setBlJoinKey_14)
  .add(googleDriveBuscarPlanillaDeAduana_7).to(googleDriveDownload_8)
  .add(googleDriveDownload_8).to(pdfExtractFromPdfAduana_9)
  .add(pdfTextoBooking_10).to(parserBookingCode_22)
  .add(downloadBooking_11).to(pdfTextoBooking_10)
  .add(buscarBookingAdviceEnDrive_12).to(downloadBooking_11)
  .add(setAduanaJoinKey_13).to(merge1BlAduana_16.input(1))
  .add(setBlJoinKey_14).to(merge1BlAduana_16)
  .add(merge1BlAduana_16).to(merge2AgregarBooking_17)
  .add(setBookingJoinKey_15).to(merge2AgregarBooking_17.input(1))
  .add(merge2AgregarBooking_17).to(comparadorBlVsAduanaVsBooking_21)
  .add(setDestinatarios_18).to(codePlantillaHtml_20)
  .add(codePlantillaHtml_20).to(sendAMessage_19)
  .add(comparadorBlVsAduanaVsBooking_21).to(setDestinatarios_18)
  .add(parserBookingCode_22).to(setBookingJoinKey_15)
  .add(pdfExtractFromPdfAduana_9).to(parserAduanaIA)
  .add(parserAduanaIA).to(inyectarCode)
  .add(inyectarCode).to(setAduanaJoinKey_13);
