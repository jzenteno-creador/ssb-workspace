# Módulo de tooling/harness n8n (control de BL) — NO es parte del front desplegado.
# Excluido del deploy Vercel para que el harness, fixtures, schemas y scripts
# no queden accesibles públicamente. El front sigue siendo index.html + assets/ + api/.
validador-aduana/

# Docs internas y maquetas (contienen órdenes/clientes reales y arquitectura) —
# material de desarrollo, NO parte del front. Sin esto quedan world-readable:
# el auth-gate es client-side de index.html y no cubre estáticos.
docs/
migrations/
